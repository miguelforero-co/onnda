---
phase: 01-redise-o-del-panel-de-configuraci-n-estilo-whisprflow
plan: 09
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05, 06, 07, 08]
files_modified:
  - src/routes/+page.svelte
autonomous: false
requirements: [Shell-sidebar, Sonidos, Pause-media, Lenguaje, Launch-at-login, Panel-de-permisos, Hotkey-recorder, Modo-atajo, Modelos-Whisper-ampliados, Check-for-updates, Gestion-de-datos, Transcripcion-por-archivos, Vista-unificada-transcripciones, Diccionario-items]
must_haves:
  truths:
    - "The full app builds (cargo + vite) and runs"
    - "Every section renders with its real content (no stubs left)"
    - "All 7 ROADMAP success criteria are observably true in the running app"
  artifacts:
    - path: "src/routes/+page.svelte"
      provides: "final wiring: file-transcribe listener, onRefresh, section data flow"
      contains: "file-transcribe-done"
  key_links:
    - from: "+page.svelte"
      to: "Transcripciones onRefresh / file-transcribe-done"
      via: "listener re-pulls history"
      pattern: "get_history"
---

<objective>
Final integration pass + a human verification checkpoint. Wire any remaining cross-section data flow in +page.svelte (notably refreshing history when a file transcription completes), do a full build, then walk the user through verifying all 7 ROADMAP success criteria in the running app.

Purpose: The feature plans built sections in isolation against contracts; this plan connects the last wires and proves the phase goal is met. This is the goal-backward gate for the whole phase.
Output: final +page.svelte wiring + a passed manual verification checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-redise-o-del-panel-de-configuraci-n-estilo-whisprflow/01-UI-SPEC.md
@src/routes/+page.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final integration wiring + full build</name>
  <read_first>
    - src/routes/+page.svelte (the shell orchestrator from 01-03 + downstream changes)
    - .planning/phases/01-redise-o-del-panel-de-configuraci-n-estilo-whisprflow/01-05-SUMMARY.md (file-transcribe event names)
  </read_first>
  <action>
In `src/routes/+page.svelte`:
- Ensure a `file-transcribe-done` listener is registered in onMount that re-pulls history (`history = await invoke("get_history")`) so a file transcription appears immediately in Transcripciones regardless of which section is active. Mirror the existing `transcription-done` listener (L83-85).
- Confirm `goHistory`/onRefresh passed to Transcripciones actually re-pulls history.
- Confirm `selected_model` selection from ModelCard persists (onSave path).
- Verify no section stub placeholder text remains (every section was filled by 01-07/01-08).
- Remove any dead code left from the monolith (old SETTINGS/HISTORY blocks fully replaced).

Run the full build to prove the whole tree (Rust + frontend) compiles together:
```bash
cargo build --manifest-path src-tauri/Cargo.toml
npm run check
npm run build
```
If `cargo test` exists for the new modules, run the suite: `cargo test --manifest-path src-tauri/Cargo.toml`.
  </action>
  <verify>
    <automated>cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -3 && npm run check 2>&1 | tail -5 && npm run build 2>&1 | tail -5 && cargo test --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "file-transcribe-done" src/routes/+page.svelte`
    - `cargo build --manifest-path src-tauri/Cargo.toml` exits 0
    - `npm run check` reports no errors
    - `npm run build` exits 0
    - `cargo test --manifest-path src-tauri/Cargo.toml` all green (settings, history, audio_decode suites)
  </acceptance_criteria>
  <done>File-transcribe completion refreshes history; full Rust + frontend build + all unit tests green; no stubs/dead code remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The complete WhisprFlow-style redesign: sidebar shell (Home/Transcripciones/Diccionario/Ajustes), all new settings, native sounds + pause-media, expanded model picker + Parakeet card, file transcription, unified transcriptions view, and the dictionary as items. The app was built; this verifies it behaves per the 7 ROADMAP success criteria.
  </what-built>
  <how-to-verify>
    Relaunch the built app (NOT dev HMR — window size + new plugin perms need a real relaunch per the project GOTCHA):
    1. **Shell (SC1):** App opens ~880×640 resizable with a left sidebar. Click each nav item — Home, Transcripciones, Diccionario, Ajustes — content swaps; active item shows the text-on-bg pill (no coral); palette is the warm beige/coral (not dark).
    2. **Sonidos (SC2):** Ajustes → enable the 3 sound toggles. Dictate (hold your hotkey): hear a cue at start, a different cue at transcribe, and a third on Escape-cancel.
    3. **Pause-media (SC3):** Play music (Music/Spotify). Enable "Pausar multimedia al grabar". Start dictation → music pauses; finish → music resumes.
    4. **Ajustes controls (SC4):** Change language; toggle push-to-talk; click the hotkey chip → press a new combo → it saves and the new hotkey works (old one no longer triggers); see ≥3 Whisper model cards with sizes + a Parakeet "Próximamente" card; toggle launch-at-login; the permissions panel shows mic + accesibilidad live (revoke one in System Settings → it flips within ~3s); "Buscar actualizaciones" returns "Estás al día" (or a version); "Abrir carpeta de datos" reveals Finder; "Borrar historial"/"Borrar modelos" each ask for confirmation.
    5. **File transcription (SC5):** Transcripciones → "Subir audio" → pick a wav/mp3/m4a → it transcribes and a new entry appears tagged as Archivo with the filename.
    6. **Unified view (SC6):** Transcripciones lists both dictation + file entries; the source filter (Todas/Dictado/Archivo) works; play + delete work.
    7. **Diccionario (SC7):** Add a word (Enter), delete a word — they persist as items (not a textarea). Dictate a phrase containing that word → Whisper biases toward it (initial_prompt still fed via custom_words).

    Report any criterion that fails with what you saw.
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 criteria pass, or describe which failed and how.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (integration only) | No new boundary; this plan verifies the boundaries handled in 01-01..01-08 hold end-to-end |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-22 | Repudiation | unverified that destructive confirms actually gate | mitigate | manual checklist step 4 explicitly verifies clear-history/clear-models prompt before acting |
</threat_model>

<verification>
- Full build (cargo + vite) + all unit tests green.
- Human checklist: all 7 ROADMAP success criteria observably true in the relaunched app.
</verification>

<success_criteria>
- The app builds and runs.
- All 7 ROADMAP Phase-1 success criteria pass human verification.
- No stub/placeholder content remains.
</success_criteria>

<output>
After completion, create `.planning/phases/01-redise-o-del-panel-de-configuraci-n-estilo-whisprflow/01-09-SUMMARY.md` (record checklist results + any follow-ups, e.g. updater endpoint still pending user_setup).
</output>
