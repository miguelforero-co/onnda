---
phase: 01-blindaje-de-producci-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/lib.rs
  - src-tauri/capabilities/default.json
autonomous: true
requirements: [HARDEN-06]
must_haves:
  truths:
    - "La app registra un logger a disco al arrancar, antes de cualquier otro plugin"
    - "El proyecto compila con las nuevas dependencias parking_lot, tauri-plugin-log y log"
  artifacts:
    - path: "src-tauri/Cargo.toml"
      provides: "Dependencias parking_lot, tauri-plugin-log, log declaradas"
      contains: "parking_lot"
    - path: "src-tauri/src/lib.rs"
      provides: "Registro del plugin de logging como PRIMER plugin de la cadena"
      contains: "tauri_plugin_log::Builder"
    - path: "src-tauri/capabilities/default.json"
      provides: "Permiso log:default"
      contains: "log:default"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "tauri_plugin_log"
      via: "tauri::Builder::default().plugin(tauri_plugin_log::Builder::new()...build())"
      pattern: "tauri_plugin_log::Builder::new"
---

<objective>
Establecer la base de robustez de la Phase 1: añadir las dependencias (`parking_lot`, `tauri-plugin-log`, `log`) y registrar el plugin de logging a disco como PRIMER plugin de la cadena del builder, con archivo rotativo en el log-dir de macOS.

Purpose: Los planes 02 y 03 convierten 8 `eprintln!` en macros `log::` y usan `parking_lot::Mutex`; ambas cosas necesitan que estas dependencias existan y que el logger esté registrado primero. Sin esto, las conversiones no compilan y los logs no llegan a disco. Este plan es la fundación de la wave.
Output: Cargo.toml con 3 deps nuevas, lib.rs con el plugin de log registrado primero, capabilities con `log:default`, todo compilando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-blindaje-de-producci-n/01-RESEARCH.md
@.planning/phases/01-blindaje-de-producci-n/01-CONTEXT.md

<interfaces>
<!-- Estado actual relevante (extraído del código). El ejecutor NO necesita explorar más. -->

src-tauri/Cargo.toml ya declara (NO duplicar):
  sha2 = "0.11"   (línea 53, ya presente, sin uso todavía)
  reqwest = { version = "0.12", default-features = false, features = ["stream", "rustls-tls"] }
  tokio = { version = "1", features = ["full"] }
  futures-util = "0.3"

src-tauri/src/lib.rs — la cadena actual de plugins empieza en línea 34:
  tauri::Builder::default()
      .plugin(tauri_plugin_opener::init())
      .plugin(tauri_plugin_autostart::init(...))
      .plugin(tauri_plugin_global_shortcut::Builder::new().build())
      ...
  El plugin de log debe insertarse ANTES de tauri_plugin_opener::init().

src-tauri/capabilities/default.json — array "permissions" (líneas 6–29), termina con "fs:default".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir dependencias parking_lot, tauri-plugin-log y log a Cargo.toml</name>
  <read_first>
    - src-tauri/Cargo.toml (estado actual; sha2 ya está en línea 53, NO añadir crates de logging duplicados)
    - .planning/phases/01-blindaje-de-producci-n/01-RESEARCH.md (sección "Standard Stack", líneas 87–92, versiones verificadas)
  </read_first>
  <files>src-tauri/Cargo.toml</files>
  <action>
    En `src-tauri/Cargo.toml`, dentro de `[dependencies]` (después de la línea 53 `sha2 = "0.11"`), añadir exactamente estas tres líneas verificadas contra crates.io 2026-06-15:
    ```toml
    # Mutex sin envenenamiento (HARDEN-02) y logging rotativo a disco (HARDEN-06).
    parking_lot = "0.12"
    tauri-plugin-log = "2"
    log = "0.4"
    ```
    NO tocar la sección `[target.'cfg(target_arch = "aarch64")'.dependencies]` ni el perfil `[profile.dev.package."*"]`. `sha2 = "0.11"` ya existe; no duplicarlo.
  </action>
  <verify>
    <automated>cd src-tauri && grep -q '^parking_lot = "0.12"' Cargo.toml && grep -q '^tauri-plugin-log = "2"' Cargo.toml && grep -q '^log = "0.4"' Cargo.toml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'parking_lot = "0.12"' src-tauri/Cargo.toml` devuelve 1
    - `grep -c 'tauri-plugin-log = "2"' src-tauri/Cargo.toml` devuelve 1
    - `grep -c 'log = "0.4"' src-tauri/Cargo.toml` devuelve 1
    - `grep -c 'sha2 = "0.11"' src-tauri/Cargo.toml` sigue devolviendo 1 (no se duplicó)
  </acceptance_criteria>
  <done>Las tres deps están declaradas una sola vez en [dependencies] sin duplicar sha2.</done>
</task>

<task type="auto">
  <name>Task 2: Registrar tauri-plugin-log como PRIMER plugin con rotación KeepOne</name>
  <read_first>
    - src-tauri/src/lib.rs (líneas 32–107, la cadena `tauri::Builder::default()` y el `.setup()`)
    - .planning/phases/01-blindaje-de-producci-n/01-RESEARCH.md (sección HARDEN-06, líneas 571–599 y 725–743; Pitfall 3 línea 643 y Pitfall 4 línea 647)
  </read_first>
  <files>src-tauri/src/lib.rs</files>
  <action>
    En `src-tauri/src/lib.rs`:
    1. Añadir el import al inicio del archivo (junto a los otros `use tauri::...`):
    ```rust
    use tauri_plugin_log::{Target, TargetKind, RotationStrategy};
    ```
    2. Dentro de `pub fn run()`, insertar el plugin de log como el PRIMER `.plugin(...)` de la cadena — ANTES de `.plugin(tauri_plugin_opener::init())` (línea 35 actual). Esto captura los logs de inicialización de los demás plugins (Pitfall 3 del research):
    ```rust
    tauri::Builder::default()
        .plugin(
            tauri_plugin_log::Builder::new()
                .level(log::LevelFilter::Info)
                .target(Target::new(TargetKind::LogDir {
                    file_name: Some("voz-local".to_string()),
                }))
                .max_file_size(5_000_000)
                .rotation_strategy(RotationStrategy::KeepOne)
                .build(),
        )
        .plugin(tauri_plugin_opener::init())
        // ... resto de plugins sin cambios
    ```
    Usar `RotationStrategy::KeepOne` — NO `KeepAll` (tiene un bug en macOS, issue tauri-apps/plugins-workspace#1397, ver Pitfall 4 del research). Límite de 5 MB por archivo. No cambiar nada más del builder ni del `.setup()`.
  </action>
  <verify>
    <automated>cd src-tauri && grep -q 'tauri_plugin_log::Builder::new' src/lib.rs && grep -q 'RotationStrategy::KeepOne' src/lib.rs && ! grep -q 'RotationStrategy::KeepAll' src/lib.rs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src-tauri/src/lib.rs` contiene `use tauri_plugin_log::{Target, TargetKind, RotationStrategy};`
    - `src-tauri/src/lib.rs` contiene `RotationStrategy::KeepOne`
    - `src-tauri/src/lib.rs` NO contiene `KeepAll`
    - El `.plugin(tauri_plugin_log::...)` aparece ANTES que `.plugin(tauri_plugin_opener::init())` en el archivo (verificable: `grep -n` del primero da una línea menor que la del segundo)
  </acceptance_criteria>
  <done>El plugin de log está registrado primero, con LogDir "voz-local", 5MB y KeepOne.</done>
</task>

<task type="auto">
  <name>Task 3: Añadir permiso log:default y verificar compilación</name>
  <read_first>
    - src-tauri/capabilities/default.json (array "permissions", líneas 6–29)
    - .planning/phases/01-blindaje-de-producci-n/01-RESEARCH.md (sección "Capabilities file update", línea 595)
  </read_first>
  <files>src-tauri/capabilities/default.json</files>
  <action>
    1. En `src-tauri/capabilities/default.json`, añadir `"log:default"` al array `permissions` (p. ej. tras `"fs:default"` en la línea 28). Cuidar la coma del JSON.
    2. Ejecutar `cd src-tauri && cargo build` para forzar la resolución de las nuevas deps y validar que el plugin compila con la firma usada. Esto descargará/compilará parking_lot, tauri-plugin-log y log por primera vez.
  </action>
  <verify>
    <automated>cd src-tauri && grep -q 'log:default' capabilities/default.json && cargo build 2>&1 | tail -5 && cargo build --quiet && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src-tauri/capabilities/default.json` contiene la cadena `log:default`
    - `cd src-tauri && cargo build` termina con exit code 0 (imprime `BUILD_OK`)
    - El JSON sigue siendo válido: `node -e "JSON.parse(require('fs').readFileSync('src-tauri/capabilities/default.json'))" && echo JSON_OK` imprime `JSON_OK`
  </acceptance_criteria>
  <done>`log:default` añadido, `cargo build` exit 0 con las tres deps nuevas resueltas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disco local → archivo de log | El texto transcrito NO debe loguearse como contenido; solo conteos/etapas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | tauri-plugin-log LogDir | accept | App 100% local, log en `~/Library/Logs/{bundle}/` solo accesible por el usuario; nivel Info no incluye contenido transcrito (los planes 02/03 loguean conteos/etapas, no texto) |
| T-01-02 | Tampering | inyección de control-chars en log | mitigate | Las conversiones de los planes 02/03 usan `log::warn!`/`log::info!` con format args (no concatenación), que escapan control chars |
</threat_model>

<verification>
- `cd src-tauri && cargo build` exit 0.
- `parking_lot`, `tauri-plugin-log`, `log` aparecen en `src-tauri/Cargo.lock` tras el build.
- El plugin de log es el primer `.plugin(...)` de la cadena en lib.rs.
</verification>

<success_criteria>
- Las 3 dependencias declaradas y resueltas (build OK).
- Logger registrado primero, archivo rotativo "voz-local" en LogDir, KeepOne, 5MB.
- `log:default` en capabilities.
</success_criteria>

<output>
After completion, create `.planning/phases/01-blindaje-de-producci-n/01-01-SUMMARY.md`
</output>
