---
phase: 01-blindaje-de-producci-n
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src-tauri/src/commands.rs
autonomous: true
requirements: [HARDEN-03, HARDEN-04, HARDEN-05, HARDEN-06]
must_haves:
  truths:
    - "El modelo se descarga desde una URL pinneada a un commit estable y se verifica por SHA256 antes de renombrar; si el hash no coincide se borra el .tmp y se devuelve error claro"
    - "Existe un comando check_model_status que el frontend puede invocar para saber proactivamente si el modelo por defecto está descargado"
    - "Un fallo de transcripción de un segmento de streaming o del tail (con segmentos ya comprometidos) emite un evento transcribe-warning en vez de descartarse en silencio"
    - "Los eprintln! de commands.rs se escriben al log a disco"
  artifacts:
    - path: "src-tauri/src/commands.rs"
      provides: "download_model con URLs pinneadas + verificación SHA256 streaming"
      contains: "expected_sha256"
    - path: "src-tauri/src/commands.rs"
      provides: "comando check_model_status + struct ModelStatus"
      contains: "pub fn check_model_status"
  key_links:
    - from: "download_model"
      to: "Sha256 hasher"
      via: "hasher.update(&chunk) en el loop de streaming + finalize comparado con expected_sha256"
      pattern: "hasher.update"
    - from: "streaming loop / tail"
      to: "frontend"
      via: "app.emit(\"transcribe-warning\", ...)"
      pattern: "transcribe-warning"
---

<objective>
Endurecer commands.rs en tres frentes y enrutar sus logs: (HARDEN-03) `download_model` descarga desde URLs pinneadas a commits inmutables de HuggingFace y verifica el SHA256 en streaming contra hashes fijados, borrando el `.tmp` y devolviendo error claro en mismatch; (HARDEN-04) un nuevo comando `check_model_status` permite al frontend detectar proactivamente al arrancar que falta el modelo; (HARDEN-05) los fallos silenciosos de un segmento de streaming y del tail (cuando ya hay segmentos comprometidos) emiten `transcribe-warning`; (HARDEN-06) los 2 `eprintln!` de commands.rs pasan a `log::`.

Purpose: Cierra el tercer crasher (descarga sin integridad), hace honesto el primer arranque sin modelo y deja de tragar fallos parciales de dictado. El comando `check_model_status` y el evento `transcribe-warning` son el contrato que consume el plan 04 (frontend).
Output: download_model reescrito con SHA256+pin, check_model_status nuevo, emits de transcribe-warning en los dos sitios de drop silencioso, 2 eprintln! convertidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-blindaje-de-producci-n/01-RESEARCH.md
@.planning/phases/01-blindaje-de-producci-n/01-CONTEXT.md

<interfaces>
<!-- Estado actual y firmas exactas de commands.rs. Usar verbatim. -->

src-tauri/src/commands.rs:
  - sha2 dep ya en Cargo.toml; CERO usos en source hoy (este plan introduce el primer uso).
  - download_model (líneas 784–837): URLs hoy flotantes `/resolve/main/...`; patrón .tmp→rename ya existe (798,834); no verifica integridad.
  - models_dir<R>(app) -> Option<PathBuf>  (línea 732)
  - settings::load(&app) -> AppSettings; campo settings.selected_model (String)
  - crate::speech_backend::APPLE_MODEL_ID
  - streaming loop drop silencioso (línea 237–243): COMMITTED_SAMPLES.fetch_add(cut,...) avanza SIEMPRE; solo `if let Ok(Ok(t))` se procesa; Ok(Err)/Err se descartan. El handle del app dentro del spawn se llama `app_for_stream` (línea 144).
  - tail drop silencioso (líneas 397–413): match tail_result { Ok(Ok(t))=>..., Ok(Err(e))=>{ if committed_text.is_empty(){emit transcribe-error; return} String::new() }, Err(e)=>{ idem } }. Aquí el handle es `app`.
  - eprintln! en commands.rs:288 (samples/rate/rms) y commands.rs:359 (streaming committed/tail summary).

Hashes y URLs pinneadas VERIFICADAS (research, x-linked-etag de HF 2026-06-15) — copiar verbatim:
  large-v3-turbo: commit 0b364b566045a405be7225ee1e415a073e04da77, file ggml-large-v3-turbo-q8_0.bin, sha256 317eb69c11673c9de1e1f0d459b253999804ec71ac4c23c17ecf5fbe24e259a1
  base:   commit 80da2d8bfee42b0e836fc3a9890373e5defc00a6, file ggml-base.bin,   sha256 60ed5bc3dd14eea856493d334349b405782ddcaf0028d4b5df4088345fba2efe
  small:  commit 80da2d8bfee42b0e836fc3a9890373e5defc00a6, file ggml-small.bin,  sha256 1be3a9b2063867b937e64e2ec7483364a79917e157fa98c5d94b5c1fffea987b
  medium: commit 80da2d8bfee42b0e836fc3a9890373e5defc00a6, file ggml-medium.bin, sha256 6c14d5adee5f86394037b4e4e8b59f1673b6cee10e3cf0b11bbdbee79c156208
URL pattern: https://huggingface.co/ggerganov/whisper.cpp/resolve/{commit}/{filename}

NOTA: el comando download_model se registra ya en lib.rs (invoke_handler). check_model_status es NUEVO → el plan 04 lo registra en el invoke_handler de lib.rs (o este plan lo añade; ver task 2).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: HARDEN-03 — URLs pinneadas + verificación SHA256 en streaming en download_model</name>
  <read_first>
    - src-tauri/src/commands.rs:784–837 (download_model completo, incluye el loop bytes_stream y el rename)
    - .planning/phases/01-blindaje-de-producci-n/01-RESEARCH.md (HARDEN-03 líneas 247–395, 709–723; Pitfalls 2/5/6 líneas 639/651/655; tabla de hashes líneas 264–269)
  </read_first>
  <files>src-tauri/src/commands.rs</files>
  <action>
    Reescribir `download_model` en `src-tauri/src/commands.rs` siguiendo el patrón del research (líneas 293–381), conservando los emits `download-progress`/`download-complete` ya existentes:
    1. Añadir al inicio del cuerpo de la función la resolución conjunta de URL + hash esperado, con las URLs pinneadas a commit y los SHA256 verbatim del bloque <interfaces> (NO usar `/resolve/main/`):
    ```rust
    use sha2::{Sha256, Digest};
    let (url, expected_sha256) = match model_id.as_str() {
        "large-v3-turbo" => ("https://huggingface.co/ggerganov/whisper.cpp/resolve/0b364b566045a405be7225ee1e415a073e04da77/ggml-large-v3-turbo-q8_0.bin", "317eb69c11673c9de1e1f0d459b253999804ec71ac4c23c17ecf5fbe24e259a1"),
        "base"  => ("https://huggingface.co/ggerganov/whisper.cpp/resolve/80da2d8bfee42b0e836fc3a9890373e5defc00a6/ggml-base.bin",  "60ed5bc3dd14eea856493d334349b405782ddcaf0028d4b5df4088345fba2efe"),
        "small" => ("https://huggingface.co/ggerganov/whisper.cpp/resolve/80da2d8bfee42b0e836fc3a9890373e5defc00a6/ggml-small.bin", "1be3a9b2063867b937e64e2ec7483364a79917e157fa98c5d94b5c1fffea987b"),
        "medium"=> ("https://huggingface.co/ggerganov/whisper.cpp/resolve/80da2d8bfee42b0e836fc3a9890373e5defc00a6/ggml-medium.bin","6c14d5adee5f86394037b4e4e8b59f1673b6cee10e3cf0b11bbdbee79c156208"),
        other   => return Err(format!("Modelo desconocido: {}", other)),
    };
    ```
    2. Limpiar cualquier `.tmp` previo antes de empezar: `let _ = tokio::fs::remove_file(&tmp).await;`
    3. En el loop `while let Some(chunk) = stream.next().await`: tras obtener el chunk, alimentar el hasher ANTES o junto al write: `hasher.update(&chunk);`. Crear el hasher antes del loop: `let mut hasher = Sha256::new();`. Si un chunk devuelve `Err` a mitad de descarga, borrar el `.tmp` antes de propagar: `.map_err(|e| { let _ = std::fs::remove_file(&tmp); e.to_string() })?`.
    4. Antes de `tokio::fs::rename`, asegurar el flush del archivo y verificar el hash (Pitfall 6: tokio File no flushea en drop): añadir `file.flush().await.map_err(|e| e.to_string())?;` luego `drop(file);`. Después:
    ```rust
    let computed = hasher.finalize().iter().map(|b| format!("{b:02x}")).collect::<String>();
    if computed != expected_sha256 {
        let _ = tokio::fs::remove_file(&tmp).await;
        return Err(format!("La descarga de '{}' está corrupta (hash incorrecto). Inténtalo de nuevo.", model_id));
    }
    ```
    5. Solo entonces `tokio::fs::rename(&tmp, &dest)...` y `app.emit("download-complete", &model_id)`.
    Usar `use sha2::{Sha256, Digest};` (NO añadir el crate `digest` ni `hex` — Pitfall 2). El `reqwest::Client` sigue el redirect 302 de HF por defecto (Pitfall 5), no cambiar la config del cliente.
  </action>
  <verify>
    <automated>cd src-tauri && grep -q 'use sha2::{Sha256, Digest};' src/commands.rs && grep -q 'expected_sha256' src/commands.rs && grep -q 'hasher.update' src/commands.rs && grep -q '317eb69c11673c9de1e1f0d459b253999804ec71ac4c23c17ecf5fbe24e259a1' src/commands.rs && ! grep -q 'resolve/main/' src/commands.rs && grep -q 'está corrupta (hash incorrecto)' src/commands.rs && cargo build --quiet && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src-tauri/src/commands.rs` contiene `use sha2::{Sha256, Digest};`, `hasher.update`, `expected_sha256` y `file.flush().await`
    - Contiene los 4 SHA256 verbatim (al menos `317eb69c...` y `60ed5bc3...` grep-verificables) y NO contiene `resolve/main/`
    - Contiene el literal `está corrupta (hash incorrecto)` y un `tokio::fs::remove_file(&tmp)` en la rama de mismatch
    - `cd src-tauri && cargo build` exit 0
  </acceptance_criteria>
  <done>download_model usa URLs pinneadas a commit + SHA256 streaming; mismatch borra .tmp y devuelve error claro; el .tmp se renombra solo tras verificar.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: HARDEN-04 — comando check_model_status para detección proactiva del modelo</name>
  <read_first>
    - src-tauri/src/commands.rs:314–348 (resolución actual de model_path y el patrón primary/fallback) y :730–782 (models_dir, get_models)
    - src-tauri/src/lib.rs:46–72 (invoke_handler — dónde registrar el comando nuevo)
    - .planning/phases/01-blindaje-de-producci-n/01-RESEARCH.md (HARDEN-04 líneas 399–461, 417–445)
  </read_first>
  <files>src-tauri/src/commands.rs, src-tauri/src/lib.rs</files>
  <action>
    1. En `src-tauri/src/commands.rs`, añadir el struct y el comando (research líneas 417–445):
    ```rust
    #[derive(serde::Serialize)]
    pub struct ModelStatus { pub ready: bool, pub model_id: String }

    #[tauri::command]
    pub fn check_model_status<R: Runtime>(app: AppHandle<R>) -> ModelStatus {
        let settings = settings::load(&app);
        let model_name = if settings.selected_model.is_empty() {
            "large-v3-turbo".to_string()
        } else { settings.selected_model.clone() };
        if model_name == crate::speech_backend::APPLE_MODEL_ID {
            return ModelStatus { ready: true, model_id: model_name };
        }
        let Some(dir) = models_dir(&app) else {
            return ModelStatus { ready: false, model_id: model_name };
        };
        let primary  = dir.join(format!("ggml-{}.bin", model_name));
        let fallback = dir.join("ggml-base.bin");
        let ready = primary.exists() || fallback.exists();
        ModelStatus { ready, model_id: model_name }
    }
    ```
    Reusa la MISMA lógica primary/fallback que ya emplea `stop_and_transcribe_internal` (no inventar nueva). Apple → siempre ready (no tiene .bin local).
    2. Registrar el comando en `src-tauri/src/lib.rs`: añadir `commands::check_model_status,` dentro del `tauri::generate_handler![...]` (junto a `commands::get_models,` / `commands::download_model,`).
  </action>
  <verify>
    <automated>cd src-tauri && grep -q 'pub fn check_model_status' src/commands.rs && grep -q 'pub struct ModelStatus' src/commands.rs && grep -q 'commands::check_model_status' src/lib.rs && cargo build --quiet && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src-tauri/src/commands.rs` contiene `pub fn check_model_status` y `pub struct ModelStatus` con campos `ready` y `model_id`
    - `src-tauri/src/lib.rs` contiene `commands::check_model_status` dentro del generate_handler
    - `cd src-tauri && cargo build` exit 0
  </acceptance_criteria>
  <done>check_model_status existe, registrado en el invoke_handler, devuelve ready/model_id reutilizando la lógica primary/fallback existente.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: HARDEN-05 + HARDEN-06 — emitir transcribe-warning en drops silenciosos y convertir eprintln!</name>
  <read_first>
    - src-tauri/src/commands.rs:235–245 (drop silencioso del segmento de streaming; handle = app_for_stream)
    - src-tauri/src/commands.rs:394–413 (drop silencioso del tail con segmentos comprometidos; handle = app)
    - src-tauri/src/commands.rs:288 y :359 (los 2 eprintln!)
    - .planning/phases/01-blindaje-de-producci-n/01-RESEARCH.md (HARDEN-05 líneas 464–557; tabla eprintln filas 2 y 3, líneas 606–607)
  </read_first>
  <files>src-tauri/src/commands.rs</files>
  <action>
    1. Sitio 1 — loop de streaming (commands.rs ~237–243). El `COMMITTED_SAMPLES.fetch_add(cut,...)` se mantiene SIEMPRE (no reprocesar audio). Reemplazar el `if let Ok(Ok(t)) = text { ... }` por un match que también maneje los caminos de error con un warning no fatal (research líneas 518–536):
    ```rust
    match text {
        Ok(Ok(t)) => {
            let t = t.trim().to_string();
            if !t.is_empty() { COMMITTED_TEXT.lock().unwrap().push(t); }
        }
        Ok(Err(e)) => {
            log::warn!("[voz-local] streaming segment failed: {e}");
            app_for_stream.emit("transcribe-warning", "Parte del dictado no pudo procesarse").ok();
        }
        Err(e) => {
            log::warn!("[voz-local] streaming segment JoinError: {e}");
            app_for_stream.emit("transcribe-warning", "Parte del dictado no pudo procesarse").ok();
        }
    }
    ```
    No abortar el loop; seguir acumulando. (Confirmar que el handle disponible en ese spawn se llama `app_for_stream`; si no, usar el clon de app que exista en ese scope.)
    2. Sitio 2 — tail con segmentos comprometidos (commands.rs ~397–413). En las ramas `Ok(Err(e))` y `Err(e)`, cuando `committed_text` NO está vacío (hoy devuelven `String::new()` callado), añadir antes del `String::new()` un log y un warning (research líneas 540–552):
    ```rust
    Ok(Err(e)) => {
        log::warn!("[voz-local] tail transcription failed: {e}");
        if committed_text.is_empty() {
            app.emit("transcribe-error", e.to_string()).ok();
            return;
        }
        app.emit("transcribe-warning", "El cierre del dictado tuvo un error parcial").ok();
        String::new()
    }
    Err(e) => {
        log::warn!("[voz-local] tail transcription JoinError: {e}");
        if committed_text.is_empty() {
            app.emit("transcribe-error", e.to_string()).ok();
            return;
        }
        app.emit("transcribe-warning", "El cierre del dictado tuvo un error parcial").ok();
        String::new()
    }
    ```
    3. eprintln! commands.rs:288 → `log::debug!("[voz-local] samples: {}, rate: {}, rms: {:.6}", samples.len(), sample_rate, rms);` (conservar args).
    4. eprintln! commands.rs:359 → `log::info!(` con el MISMO format string y args del summary de streaming (committed/tail).
  </action>
  <verify>
    <automated>cd src-tauri && [ "$(grep -c 'transcribe-warning' src/commands.rs)" -ge 3 ] && grep -q 'streaming segment failed' src/commands.rs && grep -q 'El cierre del dictado tuvo un error parcial' src/commands.rs && ! grep -q 'eprintln!' src/commands.rs && cargo build --quiet && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'transcribe-warning' src-tauri/src/commands.rs` es >= 3 (1 streaming + 2 tail)
    - Contiene los literales `Parte del dictado no pudo procesarse` y `El cierre del dictado tuvo un error parcial`
    - `grep -c 'eprintln!' src-tauri/src/commands.rs` es 0
    - El `COMMITTED_SAMPLES.fetch_add` del loop de streaming sigue presente (no se eliminó)
    - `cd src-tauri && cargo build` exit 0
  </acceptance_criteria>
  <done>Los dos sitios de drop silencioso emiten transcribe-warning; los 2 eprintln! convertidos; build OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| red (HuggingFace CDN) → archivo de modelo en disco | El binario descargado es entrada no confiable (MITM / CDN comprometido) |
| param model_id (frontend) → download_model | Entrada de usuario que selecciona el modelo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | modelo .bin corrupto/malicioso | mitigate | SHA256 streaming verificado contra hash fijado por modelo; mismatch borra .tmp, nunca renombra |
| T-01-06 | Spoofing/Tampering | URL flotante /resolve/main/ | mitigate | URL pinneada a commit SHA inmutable de HF |
| T-01-07 | Input Validation | model_id arbitrario | mitigate | allowlist `match model_id.as_str()` (ya presente, conservado) — `other => Err` |
| T-01-08 | Tampering | control-chars en log de transcripción | mitigate | logs usan `log::warn!/info!/debug!` con format args, no concatenación; no se loguea el texto transcrito como contenido |
</threat_model>

<verification>
- `cd src-tauri && cargo build` exit 0 tras los tres tasks.
- download_model: sin `resolve/main/`, con `expected_sha256`, hashes verbatim, flush+verify antes de rename.
- check_model_status registrado y compilando.
- transcribe-warning emitido en los 2 sitios; commands.rs sin eprintln!.
</verification>

<success_criteria>
- HARDEN-03: integridad SHA256 + pin de URL; mismatch → error claro + .tmp borrado.
- HARDEN-04: check_model_status disponible para el frontend.
- HARDEN-05: fallos de segmento/tail emiten transcribe-warning.
- HARDEN-06: 2 eprintln! de commands.rs convertidos.
</success_criteria>

<output>
After completion, create `.planning/phases/01-blindaje-de-producci-n/01-03-SUMMARY.md`
</output>
