---
phase: 01-blindaje-de-producci-n
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - src/routes/+page.svelte
  - src/routes/widget/+page.svelte
autonomous: false
requirements: [HARDEN-04, HARDEN-05]
must_haves:
  truths:
    - "Al abrir la ventana principal con el onboarding ya hecho pero sin el modelo por defecto descargado, el usuario ve un aviso accionable (no un fallo críptico) que lleva a descargar el modelo"
    - "Cuando el backend emite transcribe-warning, el usuario percibe que parte del dictado no se transcribió, sin que se descarte en silencio"
  artifacts:
    - path: "src/routes/+page.svelte"
      provides: "invoke check_model_status en onMount + banner accionable + listener de transcribe-warning"
      contains: "check_model_status"
    - path: "src/routes/widget/+page.svelte"
      provides: "indicador no bloqueante de transcribe-warning"
      contains: "transcribe-warning"
  key_links:
    - from: "src/routes/+page.svelte (onMount)"
      to: "comando check_model_status (commands.rs)"
      via: "invoke(\"check_model_status\")"
      pattern: "check_model_status"
    - from: "backend transcribe-warning"
      to: "widget / main UI"
      via: "listen(\"transcribe-warning\", ...)"
      pattern: "transcribe-warning"
---

<objective>
Cerrar el lazo de feedback al usuario para los dos fixes del plan 03: (HARDEN-04) al arrancar la ventana principal, invocar `check_model_status` y, si el modelo por defecto no está listo, mostrar un aviso accionable que lleve a descargarlo —en vez de dejar que el usuario descubra el fallo recién al soltar el push-to-talk; (HARDEN-05) escuchar el evento `transcribe-warning` y mostrar un indicador no bloqueante (en el widget y un aviso ligero en la ventana principal) para que un fallo parcial de transcripción sea visible.

Purpose: El backend ya emite el comando y el evento (plan 03); sin este plan los fallos siguen siendo invisibles. UX mínima y en español (constraint del proyecto), sin frameworks nuevos.
Output: +page.svelte con check_model_status + banner + listener de warning; widget con indicador de warning no bloqueante.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-blindaje-de-producci-n/01-RESEARCH.md
@.planning/phases/01-blindaje-de-producci-n/01-CONTEXT.md

<interfaces>
<!-- Contrato del backend (plan 03) + estado actual del frontend. Usar verbatim. -->

Comando backend (plan 03): invoke<{ ready: boolean; model_id: string }>("check_model_status")
Evento backend (plan 03): "transcribe-warning" con payload string (mensaje en español)

src/routes/+page.svelte (Svelte 5 runes):
  - onMount (línea 65) ya hace: settings = await invoke("get_settings"); models = await invoke("get_models"); checkPerms(); y registra listeners en `unlisten.push(await listen(...))` (líneas 81–108): transcription-done, file-transcribe-done, download-progress, download-complete.
  - Estado existente: let view = $state<View>("home"); let settings = $state<Settings>(...); let downloadProgress = $state<...>; función startDownload(modelId) (línea 132) que invoca download_model; función schedSave.
  - El shell renderiza <Home {history} /> cuando view==="home" (línea 237). settings.onboarding_done controla onboarding vs shell.
  - View type en src/lib/types.ts incluye "ajustes" (la vista de Ajustes lista/descarga modelos).

src/routes/widget/+page.svelte (Svelte 5 runes):
  - type Phase = "recording" | "transcribing" | "done" | "error"; (línea 6)
  - let phase = $state<Phase>("recording"); (línea 12)
  - const labelText = $derived(phase === "recording" ? "Listening" : phase === "transcribing" ? "Transcribing" : ""); (líneas 19–21)
  - Listeners en onMount (líneas 302–322): audio-level, audio-bands, recording-state, transcribing, transcription-done, transcribe-error (=> phase="error"; scheduleClose(1200)), recording-cancelled, screen-notch.
  - scheduleClose(delay) cierra el widget; clearTimers() existe.
  - El warning NO debe cerrar el resultado ni cambiar phase a "error": el texto ya se pegó; es solo un aviso.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HARDEN-04 — check_model_status en onMount + banner accionable en la ventana principal</name>
  <read_first>
    - src/routes/+page.svelte:1–163 (script: onMount, estado, startDownload, finishOnboarding, navegación de view)
    - src/routes/+page.svelte:230–260 (shell: render condicional por view)
    - .planning/phases/01-blindaje-de-producci-n/01-RESEARCH.md (HARDEN-04 líneas 446–461; "Where to show the banner" líneas 451–453)
  </read_first>
  <files>src/routes/+page.svelte</files>
  <action>
    En `src/routes/+page.svelte`:
    1. Añadir estado: `let modelReady = $state(true);` (asume listo hasta comprobar, para no parpadear).
    2. En `onMount`, SOLO cuando `settings.onboarding_done` es true (rama del else, línea ~75–79), invocar el comando y guardar el resultado:
    ```ts
    try {
      const st = await invoke<{ ready: boolean; model_id: string }>("check_model_status");
      modelReady = st.ready;
    } catch { modelReady = true; }  // ante error de invoke, no molestar
    ```
    No mostrar el banner durante el onboarding (ahí ya se descarga el modelo en el paso 2).
    3. Renderizar un banner no bloqueante (no modal) DENTRO del shell, encima del `<main class="content">` o al principio de la vista home, solo cuando `!modelReady`. Texto en español y acción que navegue a la vista de Ajustes (donde están los modelos):
    ```svelte
    {#if !modelReady}
      <div class="model-banner">
        <span>No hay un modelo de voz descargado. Descarga uno para empezar a dictar.</span>
        <button onclick={() => { view = "ajustes"; }}>Descargar modelo</button>
      </div>
    {/if}
    ```
    4. Cuando una descarga termina (`download-complete`, listener existente líneas 96–107), poner `modelReady = true;` para ocultar el banner sin recargar.
    5. Añadir estilos mínimos en el bloque `<style>` para `.model-banner` (no usar librerías; usar los tokens CSS existentes como `var(--accent)`, `var(--line)`). Aviso compacto, legible, descartable visualmente al descargar.
    Mantener todo lo demás del archivo sin cambios.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q 'check_model_status' src/routes/+page.svelte && grep -q 'modelReady' src/routes/+page.svelte && grep -q 'No hay un modelo de voz descargado' src/routes/+page.svelte && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `src/routes/+page.svelte` contiene `invoke<...>("check_model_status")` y el estado `modelReady`
    - Contiene el literal `No hay un modelo de voz descargado` y un botón que setea `view = "ajustes"`
    - El listener `download-complete` setea `modelReady = true`
    - `npm run check` (svelte-check) termina con exit code 0
  </acceptance_criteria>
  <done>onMount comprueba el modelo solo si onboarding_done; banner accionable aparece si falta el modelo y desaparece al descargarlo; svelte-check pasa.</done>
</task>

<task type="auto">
  <name>Task 2: HARDEN-05 — indicador no bloqueante de transcribe-warning en widget y aviso en ventana principal</name>
  <read_first>
    - src/routes/widget/+page.svelte:1–21 (Phase type, phase state, labelText derived), :299–351 (onMount listeners, scheduleClose, render del label)
    - src/routes/+page.svelte:81–108 (lista de listeners en onMount)
    - .planning/phases/01-blindaje-de-producci-n/01-RESEARCH.md (HARDEN-05 líneas 506–557; nota de UX líneas 554–557)
  </read_first>
  <files>src/routes/widget/+page.svelte, src/routes/+page.svelte</files>
  <action>
    Objetivo: que `transcribe-warning` sea VISIBLE pero NO bloquee ni cierre el resultado (el texto ya se pegó). UX de Claude (mínima, español).

    A) `src/routes/widget/+page.svelte`:
    1. Añadir estado `let warned = $state(false);` y registrar el listener en `onMount` (dentro de `unlisten.push(...)`, junto a los demás):
    ```ts
    await listen<string>("transcribe-warning", () => { warned = true; }),
    ```
    2. Que el indicador sea breve y no cambie `phase` a "error" ni dispare `scheduleClose` extra. Mínimo aceptable: reflejar el aviso en el label o con un cambio visual sutil. Ejemplo usando el `labelText` derivado — extenderlo para que, si `warned` y la fase está en transcribing/done, muestre un texto corto como "Parcial":
    ```ts
    const labelText = $derived(
      phase === "recording" ? "Listening"
      : phase === "transcribing" ? "Transcribing"
      : warned && phase === "done" ? "Parcial"
      : ""
    );
    ```
    3. Resetear `warned = false` al inicio de una nueva grabación (en el listener `recording-state` cuando `e.payload` es true, junto al `clearTimers(); phase = "recording";`).
    Conservar el comportamiento de cierre actual (no introducir cierres nuevos).

    B) `src/routes/+page.svelte`:
    4. Registrar también un listener de `transcribe-warning` en `onMount` (la ventana principal hoy no lo escucha) que muestre un aviso ligero no bloqueante (p. ej. un toast/línea que auto-desaparece a los ~4s). Usar estado `let warnMsg = $state("");` y `await listen<string>("transcribe-warning", ({ payload }) => { warnMsg = payload; setTimeout(() => { warnMsg = ""; }, 4000); });`. Renderizar `{#if warnMsg}<div class="warn-toast">{warnMsg}</div>{/if}` con estilos mínimos (tokens existentes). El texto del payload ya viene en español desde el backend.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q 'transcribe-warning' src/routes/widget/+page.svelte && grep -q 'transcribe-warning' src/routes/+page.svelte && grep -q 'warned' src/routes/widget/+page.svelte && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `src/routes/widget/+page.svelte` contiene un `listen<string>("transcribe-warning", ...)` y el estado `warned`, y NO cambia `phase` a `"error"` en ese listener
    - `src/routes/+page.svelte` contiene un `listen<string>("transcribe-warning", ...)` y un aviso que auto-desaparece
    - El listener `recording-state` del widget resetea `warned = false` al empezar una grabación
    - `npm run check` (svelte-check) termina con exit code 0
  </acceptance_criteria>
  <done>El widget y la ventana principal reflejan transcribe-warning de forma no bloqueante; svelte-check pasa.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación humana de feedback de modelo ausente y fallo parcial</name>
  <action>Punto de verificación humana (no autónomo). Tras completar Task 1 y Task 2, el ejecutor pausa aquí y presenta al usuario los pasos de how-to-verify. No hay código que escribir en este task: su único propósito es confirmar manualmente las rutas que no se pueden automatizar (micrófono desconectado a mitad de grabación, logs al lanzar desde Finder, y el feedback de modelo ausente).</action>
  <what-built>
    - HARDEN-04: al abrir la ventana principal (onboarding ya hecho) sin el modelo por defecto descargado, aparece un banner accionable "No hay un modelo de voz descargado…" que lleva a Ajustes; desaparece al descargar.
    - HARDEN-05: cuando el backend emite transcribe-warning, el widget muestra un indicador no bloqueante ("Parcial") y la ventana principal un aviso ligero que se desvanece, sin descartar el dictado en silencio.
    - (Contexto de la fase completa) HARDEN-01: desconectar el micrófono durante la grabación ya no crashea, muestra error claro.
  </what-built>
  <how-to-verify>
    Levantar el dev server: `npm run tauri dev`.
    1. HARDEN-04 (modelo ausente): cierra la app, mueve temporalmente el modelo descargado fuera de la carpeta de modelos (Ajustes → revelar datos da la ruta; o `~/Library/Application Support/{bundle}/models/`), vuelve a abrir la ventana principal. Esperado: banner accionable en español, NO un error críptico. Pulsa "Descargar modelo" → te lleva a Ajustes. Descarga uno y verifica que el banner desaparece. (Restaura el modelo movido si lo prefieres.)
    2. HARDEN-01 (mic): inicia un dictado (push-to-talk) y, mientras grabas, desconecta el micrófono USB/Bluetooth (o quítale el permiso). Esperado: la app NO crashea; ves un mensaje de error en español, no un cierre abrupto.
    3. HARDEN-06 (logs): lanza la app desde Finder (no desde terminal), usa el dictado un par de veces, y confirma que existe el log: `ls -la ~/Library/Logs/*/voz-local*` (o la ruta del bundle identifier de tauri.conf.json). Debe contener líneas [timing] y eventos.
    4. HARDEN-05 (opcional, difícil de forzar): si logras provocar un fallo parcial de transcripción, confirma que el widget muestra "Parcial"/aviso y la ventana principal el toast, sin perder el texto ya pegado.
  </how-to-verify>
  <resume-signal>Escribe "approved" o describe los problemas encontrados (capturas/mensajes).</resume-signal>
</task>

</tasks>

<verification>
- `npm run check` exit 0.
- check_model_status invocado en onMount (solo con onboarding hecho); banner accionable presente.
- transcribe-warning escuchado en widget y ventana principal, no bloqueante.
- Checkpoint humano confirma feedback de modelo ausente, no-crash de mic y logs en disco.
</verification>

<success_criteria>
- HARDEN-04: estado claro y accionable en primer arranque sin modelo (no fallo críptico).
- HARDEN-05: fallo de transcripción visible al usuario (no descartado en silencio).
- Verificación humana de las rutas no automatizables (mic, logs en Finder) aprobada.
</success_criteria>

<output>
After completion, create `.planning/phases/01-blindaje-de-producci-n/01-04-SUMMARY.md`
</output>
