---
phase: 01-redise-o-del-panel-de-configuraci-n-estilo-whisprflow
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/+page.svelte
  - src/lib/styles/tokens.css
  - src/lib/components/Sidebar.svelte
  - src/lib/components/Row.svelte
  - src/lib/components/Toggle.svelte
  - src/lib/components/PermissionRow.svelte
  - src/lib/components/ModelCard.svelte
  - src/lib/components/HotkeyRecorder.svelte
  - src/lib/sections/Home.svelte
  - src/lib/sections/Transcripciones.svelte
  - src/lib/sections/Diccionario.svelte
  - src/lib/sections/Ajustes.svelte
  - src/lib/types.ts
autonomous: true
requirements: [Shell-sidebar, Hotkey-recorder, Panel-de-permisos, Modelos-Whisper-ampliados]
must_haves:
  truths:
    - "The app shows a fixed 200px left sidebar with Home / Transcripciones / Diccionario / Ajustes nav items"
    - "Clicking a nav item swaps the content area to that section; active item shows the --text-on-bg pill (no coral)"
    - "Onboarding still runs before the shell and finishOnboarding lands on Home"
    - "The design tokens are defined once and every component reads them"
  artifacts:
    - path: "src/lib/components/Sidebar.svelte"
      provides: "200px nav rail with 4 items + wordmark/version"
      min_lines: 30
    - path: "src/lib/components/Toggle.svelte"
      provides: "extracted 36×20 toggle"
      contains: "knob"
    - path: "src/lib/components/HotkeyRecorder.svelte"
      provides: "keydown-capture combo recorder (idle/capturing/validating/saved)"
      contains: "Presiona la combinación"
    - path: "src/lib/sections/Ajustes.svelte"
      provides: "section stub mounted by router (filled in 01-07)"
    - path: "src/routes/+page.svelte"
      provides: "two-column shell + section router + onboarding"
      contains: "Sidebar"
  key_links:
    - from: "+page.svelte"
      to: "section components"
      via: "view state routes to <Home/> <Transcripciones/> <Diccionario/> <Ajustes/>"
      pattern: "view\\s*===\\s*\"(home|ajustes)\""
    - from: "components"
      to: "tokens.css"
      via: "global import of design tokens"
      pattern: "tokens"
---

<objective>
Replace the header+tabs monolith with a WhisprFlow-style two-column shell (200px sidebar + content area), extract the inline design system into reusable Svelte 5 components, and stand up empty section stubs + a section router so the feature plans (01-07/01-08) can fill sections WITHOUT editing +page.svelte.

Purpose: D-01/D-02/D-06 — the structural redesign. The current `+page.svelte` IS the design system; this plan hoists tokens to a shared stylesheet, extracts Row/Toggle/PermissionRow/ModelCard/HotkeyRecorder verbatim from the inline CSS, and builds the sidebar nav. It preserves the palette (D-04 LOCKED) and keeps the onboarding flow intact.
Output: `src/lib/styles/tokens.css`, six components under `src/lib/components/`, four section stubs under `src/lib/sections/`, a shared `types.ts`, and a rewritten `+page.svelte` orchestrator that owns settings/models/history state + onboarding + section routing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-redise-o-del-panel-de-configuraci-n-estilo-whisprflow/01-UI-SPEC.md
@.planning/phases/01-redise-o-del-panel-de-configuraci-n-estilo-whisprflow/01-PATTERNS.md
@src/routes/+page.svelte

<interfaces>
<!-- The full current monolith is in context above. Extract these verbatim: -->
Tokens (:root L488-499): --bg #F4F0EB, --panel #FDFCFA, --text #1C1917, --muted #78716C, --faint #A8A29E, --line rgba(0,0,0,0.07), --coral #E85535, --amber #F4AA6A, --blue #7B9BD2, --r 9px.
Toggle markup L321-327 / CSS L596-609 (36×20, knob 16, .toggle.on → --coral track, translateX(16px)).
Row block L561-577 (.rows panel/radius/overflow, .sep 1px line margin 0 12px, .row padding 10px 14px min-height 42px).
PermissionRow L204-228 markup + L626-646 CSS (dot+info+status/action; granted → --blue).
ModelCard L404-431 markup + L749-760 dl-bar CSS (Installed badge --blue; Descargar link-btn; dl-bar coral).
HotkeyRecorder: replaces shortcut <input> L311-318; kbd styling L730-734; capture shape from RESEARCH.md L264-283.
Orchestration to KEEP in +page.svelte: state+invoke/listen L25-104, schedSave L113-119, checkPerms L121-124, finishOnboarding L138-144.
Settings/HistoryEntry/ModelInfo/DownloadProgress TS interfaces L7-23.
Active-nav css: .tab.on{ color:var(--text); background:var(--bg); } radius 6px (NO coral on inactive).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract design tokens + shared TS types, build reusable components</name>
  <read_first>
    - src/routes/+page.svelte (the entire file — it is the design-system source of truth)
    - .planning/phases/01-redise-o-del-panel-de-configuraci-n-estilo-whisprflow/01-UI-SPEC.md (tokens table, spacing, typography, color contract)
  </read_first>
  <action>
Create `src/lib/styles/tokens.css` with the `:root` token block VERBATIM from +page.svelte L488-499 (--bg, --panel, --text, --muted, --faint, --line, --coral, --amber, --blue, --r) plus the `:global(*)` reset and `:global(body)` font block (L501-510). This becomes the single source; +page.svelte imports it (`import "$lib/styles/tokens.css";` in its `<script>`).

Create `src/lib/types.ts` exporting the interfaces from +page.svelte L7-23, EXTENDED to match the new Rust structs (plan 01-01):
```ts
export interface Settings {
  shortcut: string; push_to_talk: boolean; selected_language: string;
  selected_model: string; autostart: boolean; onboarding_done: boolean;
  widget_position: string; custom_words: string; word_correction_threshold: number;
  sound_on_listen: boolean; sound_on_stop: boolean; sound_on_cancel: boolean;
  pause_media: boolean; dictionary: string[];
}
export interface HistoryEntry {
  id: string; timestamp_ms: number; text: string;
  audio_filename: string | null; duration_secs: number;
  source: string; original_filename: string | null;
}
export interface ModelInfo { id: string; name: string; size_mb: number; downloaded: boolean; }
export interface DownloadProgress { model_id: string; downloaded_mb: number; total_mb: number; percent: number; }
export type View = "onboarding" | "home" | "transcripciones" | "diccionario" | "ajustes";
```

Create components under `src/lib/components/` using Svelte 5 runes (`$props()`), each with a scoped `<style>` copying the EXACT CSS from the analog lines. Use `currentColor` SVG icons hand-written (per CLAUDE.md — never image models), 16px stroke 1.5:
- `Toggle.svelte`: props `{ checked = $bindable(), label, id, onchange }`. Markup L321-327, CSS L596-609. Keep 36×20 / knob 16 (UI-SPEC: do not alter).
- `Row.svelte`: a generic row wrapper — props `{ label }` + a default `<slot/>` for the control on the right. CSS from `.row` L569-577. Also export `.rows`/`.sep` as a `Rows.svelte` OR document that section components wrap rows in `<div class="rows">` with `<div class="sep">` separators (keep values: padding 10px 14px, min-height 42px, sep 1px var(--line) margin 0 12px).
- `PermissionRow.svelte`: props `{ label, description, granted, onOpen }`. Markup L204-228, CSS L626-646. Granted → --blue dot + "Concedido"; else muted dot + "Abrir ajustes" link-btn (--coral).
- `ModelCard.svelte`: props `{ model: ModelInfo, selected, progress, error, comingSoon = false, onDownload, onSelect }`. Markup L404-431 + dl CSS L749-760. States: Installed (--blue badge), Downloading (dl-bar + %), Descargar (link-btn), Selected (1px coral ring on card). When `comingSoon`: render disabled/muted with badge "Próximamente" + subtext "Disponible en una próxima versión." and NO coral (UI-SPEC color contract, D-13).
- `HotkeyRecorder.svelte`: props `{ shortcut = $bindable(), onCommitted }`. States per UI-SPEC L158-163: Idle (kbd chip showing combo, L730-734 styling, click → Capturing), Capturing (pulsing coral-bordered field, text "Presiona la combinación…", listens keydown), Validating (reject combos with no modifier → inline hint "Usa al menos un modificador (⌘/⌥/⌃)"), Saved (back to Idle, call `onCommitted(newCombo)`). Use the captureCombo shape (RESEARCH L264-283): map metaKey→"CommandOrControl", altKey→"Alt", shiftKey→"Shift", ctrlKey→"Control"; ignore lone modifiers; Escape cancels. GOTCHA (UI-SPEC L163 / PATTERNS): NEVER register mid-capture — the parent calls schedSave(true) only after onCommitted fires. Default combo "Alt+Space".
  </action>
  <verify>
    <automated>npm run check 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/lib/styles/tokens.css && grep -q "#E85535" src/lib/styles/tokens.css`
    - `test -f src/lib/types.ts && grep -q "dictionary: string\[\]" src/lib/types.ts && grep -q "source: string" src/lib/types.ts`
    - `test -f src/lib/components/Toggle.svelte && grep -q "knob" src/lib/components/Toggle.svelte`
    - `test -f src/lib/components/PermissionRow.svelte && grep -q "Concedido" src/lib/components/PermissionRow.svelte`
    - `test -f src/lib/components/ModelCard.svelte && grep -q "Próximamente" src/lib/components/ModelCard.svelte`
    - `test -f src/lib/components/HotkeyRecorder.svelte && grep -q "Presiona la combinación" src/lib/components/HotkeyRecorder.svelte`
    - `npm run check` reports no svelte-check errors in the new component files
  </acceptance_criteria>
  <done>tokens.css + types.ts exist; Toggle, Row, PermissionRow, ModelCard, HotkeyRecorder components exist with verbatim CSS values and Spanish microcopy; svelte-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build the sidebar + rewrite +page.svelte as shell orchestrator with section router and stubs</name>
  <read_first>
    - src/routes/+page.svelte (the whole monolith — you are restructuring it)
    - .planning/phases/01-redise-o-del-panel-de-configuraci-n-estilo-whisprflow/01-UI-SPEC.md L44-51 (layout shell spec)
  </read_first>
  <action>
Create `src/lib/components/Sidebar.svelte`: a fixed 200px `--panel` rail with a `1px solid var(--line)` right border. Top = wordmark "Voz Local" + version (reuse `.wordmark`/`.version` styles L523-530). Below = four nav buttons (icon 16px + label 13px): Home, Transcripciones, Diccionario, Ajustes. Props `{ view = $bindable() }`. Active item = `--text` on a `--bg` pill, radius 6px (mirror `.tab.on` L540); rest `--faint`, hover `--muted`. NO coral on inactive nav (UI-SPEC). Hand-write four inline SVG icons (home, list/transcript, book/dictionary, gear/settings) stroke 1.5 currentColor.

Create FOUR empty section stubs under `src/lib/sections/` so the router compiles and 01-07/01-08 fill them without touching +page.svelte:
- `Home.svelte`, `Transcripciones.svelte`, `Diccionario.svelte`, `Ajustes.svelte`.
Each stub: a `<script>` declaring the `$props()` it will receive (pass the orchestrator state down) + a placeholder `<h1 class="page-title">{Title}</h1>` (16px/600 heading per UI-SPEC typography). Define the prop surface NOW so downstream plans implement against a contract:
  - Home: `{ settings, history, onNavigate }`
  - Transcripciones: `{ history, onRefresh }`
  - Diccionario: `{ settings, onSave }`
  - Ajustes: `{ settings, models, downloadProgress, downloadErrors, micGranted, a11yGranted, onSave, onDownload, onCheckPerms }`

Rewrite `src/routes/+page.svelte`:
- Keep ALL orchestration: the state ($state for settings/models/history/perms/downloadProgress/downloadErrors/appVersion/buildHash), `onMount` invoke+listen wiring (L66-104), `schedSave` (L113-119), `checkPerms` (L121-124), `startDownload`, `finishOnboarding`, `deleteEntry`, `playAudio`, `fmtTime`, `fmtDur`. Import `Settings/View` from `$lib/types` and `import "$lib/styles/tokens.css"`.
- Change `view` to type `View` (default "home" after onboarding). `finishOnboarding` sets `view = "home"` (was "settings").
- Onboarding (L189-287) stays as-is (it precedes the shell) but uses the extracted PermissionRow + ModelCard components.
- Replace the `header`+`.tabs`+`SETTINGS`+`HISTORY` blocks with the two-column shell:
```svelte
{#if view === "onboarding"}
  <!-- existing onboarding markup -->
{:else}
  <div class="shell">
    <Sidebar bind:view />
    <main class="content">
      {#if view === "home"}<Home {settings} {history} onNavigate={(v) => view = v} />{/if}
      {#if view === "transcripciones"}<Transcripciones {history} onRefresh={goHistory} />{/if}
      {#if view === "diccionario"}<Diccionario {settings} onSave={() => schedSave()} />{/if}
      {#if view === "ajustes"}<Ajustes {settings} {models} {downloadProgress} {downloadErrors} {micGranted} {a11yGranted} onSave={(sc) => schedSave(sc)} onDownload={startDownload} onCheckPerms={checkPerms} />{/if}
    </main>
  </div>
{/if}
```
- `.shell{ display:flex; height:100vh; }` `.content{ flex:1; overflow-y:auto; background:var(--bg); padding:32px; }` (xl gutter per UI-SPEC spacing). `.page-title{ font-size:16px; font-weight:600; }` can live in a shared spot or each section.
- Remove the now-unused inline token block / reset (now in tokens.css) and the extracted-component CSS that moved out. Keep onboarding CSS that's still used.

Keep the `transcription-done` listener updating history when on the transcripciones view.
  </action>
  <verify>
    <automated>npm run check 2>&1 | tail -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/lib/components/Sidebar.svelte && grep -q "Transcripciones" src/lib/components/Sidebar.svelte && grep -q "Diccionario" src/lib/components/Sidebar.svelte`
    - `test -f src/lib/sections/Home.svelte && test -f src/lib/sections/Transcripciones.svelte && test -f src/lib/sections/Diccionario.svelte && test -f src/lib/sections/Ajustes.svelte`
    - `grep -q "Sidebar" src/routes/+page.svelte && grep -q 'view === "home"' src/routes/+page.svelte && grep -q 'view === "ajustes"' src/routes/+page.svelte`
    - `grep -q 'view = "home"' src/routes/+page.svelte` (finishOnboarding lands on Home)
    - `grep -q 'import "\$lib/styles/tokens.css"' src/routes/+page.svelte`
    - `npm run check` clean; `npm run build` succeeds
  </acceptance_criteria>
  <done>Sidebar renders 4 nav items with correct active affordance; +page.svelte is a shell orchestrator routing to four section stubs; onboarding intact and lands on Home; tokens shared; build + check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | This plan is frontend structure only; no new trust boundary — same invoke surface as today |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | rendering history.text (user/transcribed content) | mitigate | Svelte escapes interpolated `{e.text}` by default (no `{@html}`) — XSS not reachable; keep using `{text}` not `{@html}` |
</threat_model>

<verification>
- `npm run check` and `npm run build` succeed.
- Manual (deferred to 01-09 checklist): sidebar shows 4 items, nav switches sections, active pill correct, onboarding → Home.
</verification>

<success_criteria>
- Two-column shell (200px sidebar + content) replaces header+tabs.
- Tokens extracted to one stylesheet (palette LOCKED, D-04).
- Reusable components: Toggle, Row, PermissionRow, ModelCard, HotkeyRecorder.
- Four section stubs with defined prop contracts + a working router.
- Onboarding preserved, lands on Home.
</success_criteria>

<output>
After completion, create `.planning/phases/01-redise-o-del-panel-de-configuraci-n-estilo-whisprflow/01-03-SUMMARY.md` (list the component prop contracts so 01-07/01-08 implement against them).
</output>
