---
phase: 01-redise-o-del-panel-de-configuraci-n-estilo-whisprflow
plan: 07
type: execute
wave: 5
depends_on: [01, 03, 04, 06]
files_modified:
  - src/lib/sections/Ajustes.svelte
  - src/lib/types.ts
autonomous: true
requirements: [Sonidos, Pause-media, Lenguaje, Launch-at-login, Panel-de-permisos, Hotkey-recorder, Modo-atajo, Modelos-Whisper-ampliados, Check-for-updates, Gestion-de-datos]
must_haves:
  truths:
    - "Ajustes shows: hotkey recorder, push-to-talk toggle, language select"
    - "Ajustes shows 3 independent sound toggles + a pause-media toggle"
    - "Ajustes shows launch-at-login toggle and a live permissions panel (mic + a11y)"
    - "Ajustes shows the expanded model cards + the Parakeet Próximamente card"
    - "Ajustes shows check-for-updates (with result) and data-management actions with confirmation"
  artifacts:
    - path: "src/lib/sections/Ajustes.svelte"
      provides: "the full settings section wired to backend commands"
      min_lines: 120
      contains: "HotkeyRecorder"
  key_links:
    - from: "Ajustes toggles/selects"
      to: "settings + onSave (schedSave)"
      via: "bind + onchange"
      pattern: "onSave"
    - from: "Ajustes data-mgmt buttons"
      to: "clear_history/clear_models/reveal_data_dir"
      via: "invoke after confirm"
      pattern: "clear_history|clear_models|reveal_data_dir"
    - from: "Ajustes updates"
      to: "check_for_updates"
      via: "invoke"
      pattern: "check_for_updates"
---

<objective>
Implement the Ajustes section by filling the stub created in 01-03, wiring every new setting to the backend that Wave 2-4 built: hotkey recorder, push-to-talk, language, the 3 sound toggles + pause-media, launch-at-login, permissions panel, expanded model cards + Parakeet, check-for-updates, and data management.

Purpose: D-07 through D-15 — the settings surface. This consumes the extracted components (Toggle, PermissionRow, ModelCard, HotkeyRecorder from 01-03) and the commands (transcribe none here; check_for_updates, clear_*, reveal from 01-06; expanded get_models from 01-04). It does NOT edit +page.svelte — it implements against the prop contract 01-03 defined.
Output: a complete `Ajustes.svelte` grouped into sections (Grabación / Reconocimiento / Sonidos / Sistema / Permisos / Modelos / Actualizaciones / Datos), all in Spanish, palette LOCKED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-redise-o-del-panel-de-configuraci-n-estilo-whisprflow/01-UI-SPEC.md
@.planning/phases/01-redise-o-del-panel-de-configuraci-n-estilo-whisprflow/01-CONTEXT.md
@src/routes/+page.svelte

<interfaces>
<!-- Prop contract from 01-03 (Ajustes stub): -->
Ajustes props: { settings, models, downloadProgress, downloadErrors, micGranted, a11yGranted, onSave(scChanged?), onDownload(modelId), onCheckPerms() }
Components available ($lib/components): Toggle { checked=$bindable, label, id, onchange }, Row { label }+slot, PermissionRow { label, description, granted, onOpen }, ModelCard { model, selected, progress, error, comingSoon, onDownload, onSelect }, HotkeyRecorder { shortcut=$bindable, onCommitted }.
Backend commands: invoke("check_for_updates") -> { up_to_date, available_version, current_version, error }; invoke("reveal_data_dir"); invoke("clear_history"); invoke("clear_models"); invoke("open_microphone_settings"); invoke("open_accessibility_settings").
Settings fields (types.ts): shortcut, push_to_talk, selected_language, selected_model, autostart, sound_on_listen, sound_on_stop, sound_on_cancel, pause_media, custom_words, word_correction_threshold, dictionary[].
ModelInfo now has coming_soon: bool (add to TS interface).
LANGUAGES list (+page.svelte L50-57): auto/es/en/pt/fr/de.
UI-SPEC copy: Updates up-to-date "Estás al día"; available "Hay una versión nueva disponible (vX.Y.Z)"; data confirms L148-149; permissions Concedido/Abrir ajustes; hotkey idle "Atajo de teclado".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add coming_soon to ModelInfo TS + build Ajustes recording/recognition/sounds/system sections</name>
  <read_first>
    - src/lib/sections/Ajustes.svelte (stub from 01-03)
    - src/lib/types.ts (ModelInfo interface)
    - src/routes/+page.svelte L307-399 (existing Grabación/Reconocimiento/Interfaz/Sistema markup to adapt)
    - src/lib/components/Toggle.svelte, HotkeyRecorder.svelte, PermissionRow.svelte (the components to consume)
  </read_first>
  <action>
Add `coming_soon: boolean;` to the `ModelInfo` interface in `src/lib/types.ts`.

In `Ajustes.svelte`, declare `$props()` matching the contract. Add a page title `<h1 class="page-title">Ajustes</h1>` (16px/600). Use `<section>`+`.section-label`+`.rows`/`.sep`/`.row` structure (existing classes; reference tokens.css). Build these groups:

**Grabación:** 
- HotkeyRecorder row: `<HotkeyRecorder bind:shortcut={settings.shortcut} onCommitted={() => onSave(true)} />` (label "Atajo de teclado"). `onSave(true)` triggers shortcutChanged → re_register (GOTCHA: only after capture completes, never mid-capture).
- Push-to-talk Toggle (D-12): `<Toggle bind:checked={settings.push_to_talk} id="ptt" label="Push to talk" onchange={() => onSave()} />` + the section-hint that flips text (L329).

**Reconocimiento:**
- Idioma select (bind selected_language, onchange onSave) from LANGUAGES.
- Modelo select limited to downloaded models (existing L342-349) OR rely on the Modelos cards below for selection — keep a compact "modelo activo" select bound to selected_model among downloaded.

**Sonidos (D-07):** three independent Toggles:
- `sound_on_listen` label "Sonido al escuchar"
- `sound_on_stop` label "Sonido al transcribir"
- `sound_on_cancel` label "Sonido al cancelar"
plus pause-media (D-08) Toggle `pause_media` label "Pausar multimedia al grabar". All `onchange={() => onSave()}`.

**Sistema:** launch-at-login (D-10) Toggle `autostart` label "Iniciar con el sistema" `onchange={() => onSave()}` (already wired in save_settings — this just surfaces it).
  </action>
  <verify>
    <automated>npm run check 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "coming_soon: boolean" src/lib/types.ts`
    - `grep -q "HotkeyRecorder" src/lib/sections/Ajustes.svelte`
    - `grep -q "sound_on_listen" src/lib/sections/Ajustes.svelte && grep -q "sound_on_stop" src/lib/sections/Ajustes.svelte && grep -q "sound_on_cancel" src/lib/sections/Ajustes.svelte`
    - `grep -q "pause_media" src/lib/sections/Ajustes.svelte`
    - `grep -q "autostart" src/lib/sections/Ajustes.svelte`
    - `grep -q "onSave(true)" src/lib/sections/Ajustes.svelte` (shortcut change)
    - `npm run check` clean
  </acceptance_criteria>
  <done>Ajustes has Grabación (hotkey recorder + push-to-talk), Reconocimiento (language + active model), Sonidos (3 toggles + pause-media), Sistema (launch-at-login), all wired to onSave; svelte-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build Ajustes permissions panel, model cards, updates, and data-management sections</name>
  <read_first>
    - src/lib/sections/Ajustes.svelte (after Task 1)
    - src/lib/components/PermissionRow.svelte, ModelCard.svelte
    - src/routes/+page.svelte L401-433 (existing model rows), L204-228 (perm rows)
    - .planning/phases/01-redise-o-del-panel-de-configuraci-n-estilo-whisprflow/01-UI-SPEC.md L143-167 (destructive confirmations, model card states, permissions)
  </read_first>
  <action>
**Permisos (D-09):** a section using PermissionRow for Micrófono ({granted: micGranted, onOpen: () => invoke("open_microphone_settings")}) and Accesibilidad ({granted: a11yGranted, onOpen: () => invoke("open_accessibility_settings")}). The parent (+page.svelte) already polls checkPerms every 3s and passes micGranted/a11yGranted down reactively — call `onCheckPerms()` on mount of this section to refresh immediately.

**Modelos (D-13):** iterate `models` rendering `<ModelCard>` for each. For real models pass `comingSoon={false}`, `selected={settings.selected_model === m.id}`, `progress={downloadProgress[m.id]}`, `error={downloadErrors[m.id]}`, `onDownload={() => onDownload(m.id)}`, `onSelect={() => { settings.selected_model = m.id; onSave(); }}`. For the Parakeet entry (`m.coming_soon`) pass `comingSoon={true}` so the card renders disabled/muted with the "Próximamente" badge + "Disponible en una próxima versión." (no coral, no select/download). Selected card = 1px coral ring.

**Actualizaciones (D-14):** a row with a `.link-btn` "Buscar actualizaciones" that calls:
```ts
let updateMsg = $state("");
async function checkUpdates() {
  const st = await invoke("check_for_updates");
  updateMsg = st.up_to_date
    ? "Estás al día"
    : `Hay una versión nueva disponible (v${st.available_version})`;
}
```
Show `updateMsg` beside the button. (Current version can show from st.current_version.)

**Datos (D-15):** three actions:
- "Abrir carpeta de datos" → `invoke("reveal_data_dir")`.
- "Borrar historial y audios" → confirm via `window.confirm("¿Borrar todo el historial y los audios guardados? Esta acción no se puede deshacer.")` then `invoke("clear_history")`. (UI-SPEC L148.)
- "Borrar modelos descargados" → confirm `window.confirm("¿Borrar los modelos descargados? Tendrás que volver a descargarlos para dictar.")` then `invoke("clear_models")` then refresh models if a refresh callback exists (or re-invoke get_models locally). (UI-SPEC L149.)
Destructive confirm buttons use coral per UI-SPEC; `window.confirm` is acceptable for Phase 1 (native confirm). Both destructive actions MUST be behind the confirm.

All copy in Spanish, palette LOCKED (no new colors). Coral only on the explicit reserved list (UI-SPEC L104-110): link-btns, toggle-on, selected-card ring, primary CTA.
  </action>
  <verify>
    <automated>npm run check 2>&1 | tail -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "PermissionRow" src/lib/sections/Ajustes.svelte`
    - `grep -q "ModelCard" src/lib/sections/Ajustes.svelte && grep -q "comingSoon" src/lib/sections/Ajustes.svelte`
    - `grep -q "check_for_updates" src/lib/sections/Ajustes.svelte && grep -q "Estás al día" src/lib/sections/Ajustes.svelte`
    - `grep -q "reveal_data_dir" src/lib/sections/Ajustes.svelte`
    - `grep -q "clear_history" src/lib/sections/Ajustes.svelte && grep -q "clear_models" src/lib/sections/Ajustes.svelte`
    - `grep -q "no se puede deshacer" src/lib/sections/Ajustes.svelte` (confirm gating)
    - `npm run check` clean; `npm run build` succeeds
  </acceptance_criteria>
  <done>Permissions panel (live), model cards (incl. Parakeet coming-soon + selection), check-for-updates with Spanish result, and confirmed data-management actions all present and wired; build + check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → destructive command | clear_history/clear_models are irreversible; UI is the confirmation gate |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17 | Tampering | accidental data wipe with no confirmation | mitigate | both destructive actions behind window.confirm with explicit Spanish "no se puede deshacer" copy (UI-SPEC L148-149) before invoke |
| T-01-18 | Tampering | rendering update version string | mitigate | interpolated as text (Svelte escapes); no {@html} |
</threat_model>

<verification>
- `npm run check` and `npm run build` succeed.
- Manual (01-09): every Ajustes control persists; permissions update live; model cards show states; updates returns a message; data actions confirm then act.
</verification>

<success_criteria>
- All settings from D-07..D-15 are present and wired in Ajustes.
- Parakeet renders as a muted Próximamente card (no coral, not selectable).
- Destructive data actions are confirmation-gated.
- Spanish copy; palette LOCKED.
</success_criteria>

<output>
After completion, create `.planning/phases/01-redise-o-del-panel-de-configuraci-n-estilo-whisprflow/01-07-SUMMARY.md`
</output>
