---
phase: 02-compatibilidad-honesta-intel-apple-silicon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-sidecar.sh
  - .github/workflows/release.yml
  - src-tauri/tauri.conf.json
autonomous: true
requirements: [COMPAT-01]
must_haves:
  truths:
    - "El sidecar Swift se cross-compila a x86_64 y el archivo asr-x86_64-apple-darwin se produce"
    - "build-sidecar.sh genera AMBOS triples (aarch64 + x86_64) en src-tauri/binaries/"
    - "El workflow de release corre en macos-15 (Xcode 26.x, SDK macOS 26)"
    - "CI compila el sidecar para el triple correcto antes de cada tauri build"
  artifacts:
    - path: "scripts/build-sidecar.sh"
      provides: "Build de ambos triples del sidecar"
      contains: "x86_64-apple-macosx11.0"
    - path: ".github/workflows/release.yml"
      provides: "CI con runner macos-15 + steps de build del sidecar"
      contains: "macos-15"
    - path: "src-tauri/binaries/asr-x86_64-apple-darwin"
      provides: "Binario sidecar x86_64 (Mach-O x86_64)"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "scripts/build-sidecar.sh (o swift build inline)"
      via: "step que compila ambos triples antes del primer tauri build"
      pattern: "swift build.*release"
---

<objective>
Hacer que el bundle Intel (x86_64) se construya en CI produciendo el binario x86_64 del
sidecar Swift, para que `externalBin: ["binaries/asr"]` resuelva en el build
`--target x86_64-apple-darwin`. Hoy `build-sidecar.sh` solo compila el host-triple
(aarch64) y `release.yml` ni siquiera invoca el build del sidecar.

Purpose: Sin esto, el DMG de Intel falla con `"binaries/asr-x86_64-apple-darwin" not found`.
Es el bloqueador #1 del lanzamiento en la flota Intel (COMPAT-01).
Output: build-sidecar.sh dual-triple, release.yml en macos-15 con steps de sidecar, ambos
binarios presentes en src-tauri/binaries/.

NOTA DE VERIFICACIÓN: NO tenemos hardware Intel y NO podemos bootear Intel. La verificación
local de COMPAT-01 es que el cross-compile x86_64 produzca el binario (`file` reporta x86_64)
y que los cambios de script/CI/config estén presentes. El DMG Intel completo se verifica en CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-compatibilidad-honesta-intel-apple-silicon/02-RESEARCH.md
@.planning/phases/02-compatibilidad-honesta-intel-apple-silicon/02-CONTEXT.md

<facts>
<!-- Hechos verificados en RESEARCH.md (HIGH confidence, ejecutados en la sesión de research) -->
- Comando cross-compile verificado: `swift build -c release --triple x86_64-apple-macosx11.0`
  desde `src-tauri/sidecar-asr` produce `.build/x86_64-apple-macosx/release/asr` (Mach-O x86_64, ~50s).
- El build arm64 nativo (`swift build -c release`) produce `.build/arm64-apple-macosx/release/asr`.
- Tauri resuelve el sidecar por target-triple del build: `--target x86_64-apple-darwin` →
  busca `src-tauri/binaries/asr-x86_64-apple-darwin`; build nativo arm64 → `asr-aarch64-apple-darwin`.
- El `minos` del binario x86_64 será 26.0 (lo fija Package.swift `.macOS("26.0")`) — esto es
  CORRECTO e intencional. En Intel el sidecar nunca se ejecuta (gateado por COMPAT-02). NO es un error.
- runner `macos-14` (actual) tiene Xcode 15.4 (SDK 14) → `swift build` FALLA con
  "cannot find type 'SpeechTranscriber'". Hay que cambiar a `macos-15` (Xcode 26.3, SDK 26).
- AMBOS triples deben existir ANTES del primer `tauri build` — Tauri verifica todos los
  externalBin al inicio del bundle. Producirlos en dos steps consecutivos antes de cualquier build.
- `rustup target add x86_64-apple-darwin` ya está en release.yml:29 — no tocar.
</facts>

<current_state>
scripts/build-sidecar.sh (estado actual — solo host-triple):
  line 11: TRIPLE="$(rustc --print host-tuple)"
  line 14: ( cd "$SIDECAR_DIR" && swift build -c release )
  line 17: cp -f "$SIDECAR_DIR/.build/release/asr" "$OUT_DIR/asr-$TRIPLE"

.github/workflows/release.yml:
  line 12: runs-on: macos-14            ← cambiar a macos-15
  line 28-29: Add x86_64 cross-compilation target (rustup target add) ← conservar
  line 41-42: Build arm64 app bundle (npm run tauri build -- --bundles app)  ← antes de esto va el sidecar arm64
  line 65-66: Build x86_64 app bundle (--target x86_64-apple-darwin)  ← antes va el sidecar x86_64
  NOTA: hoy NO hay ningún step que compile el sidecar. Hay que añadirlos.

src-tauri/tauri.conf.json:
  line 51: "targets": ["dmg", "app"]
  line 52: "externalBin": ["binaries/asr"]
  line 61: "minimumSystemVersion": "11.0"
</current_state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: build-sidecar.sh produce ambos triples</name>
  <files>scripts/build-sidecar.sh</files>
  <read_first>
    scripts/build-sidecar.sh (archivo a modificar — estado actual arriba en current_state).
    RESEARCH.md sección "Actualizar scripts/build-sidecar.sh" y "Code Examples → COMPAT-01" (líneas 545-565)
    contienen el script final exacto a usar como referencia.
  </read_first>
  <action>
    Reescribir scripts/build-sidecar.sh para producir AMBOS triples. Eliminar la línea
    `TRIPLE="$(rustc --print host-tuple)"` (ya no se usa un solo triple). Mantener `set -euo pipefail`,
    `ROOT`, `SIDECAR_DIR`, `OUT_DIR`.

    Sección arm64 (nativo) — usar la ruta `.build/arm64-apple-macosx/release/asr` (NO `.build/release/asr`,
    que es un symlink que puede no resolver tras un cross-build):
      ( cd "$SIDECAR_DIR" && swift build -c release )
      mkdir -p "$OUT_DIR"
      cp -f "$SIDECAR_DIR/.build/arm64-apple-macosx/release/asr" "$OUT_DIR/asr-aarch64-apple-darwin"
      chmod +x "$OUT_DIR/asr-aarch64-apple-darwin"

    Sección x86_64 (cross-compiled) — comando EXACTO verificado:
      ( cd "$SIDECAR_DIR" && swift build -c release --triple x86_64-apple-macosx11.0 )
      cp -f "$SIDECAR_DIR/.build/x86_64-apple-macosx/release/asr" "$OUT_DIR/asr-x86_64-apple-darwin"
      chmod +x "$OUT_DIR/asr-x86_64-apple-darwin"

    Añadir un `echo` informativo tras cada cp ("Placed: ..."). El script debe ser ejecutable
    sin argumentos desde la raíz del repo (`bash scripts/build-sidecar.sh`).
  </action>
  <acceptance_criteria>
    - `grep -c "swift build -c release" scripts/build-sidecar.sh` devuelve 2
    - `grep -q "x86_64-apple-macosx11.0" scripts/build-sidecar.sh` (cross-compile presente)
    - `grep -q "asr-aarch64-apple-darwin" scripts/build-sidecar.sh` && `grep -q "asr-x86_64-apple-darwin" scripts/build-sidecar.sh`
    - `! grep -q "rustc --print host-tuple" scripts/build-sidecar.sh` (línea del host-triple eliminada)
    - Ejecutar `bash scripts/build-sidecar.sh` completa sin error (exit 0) y deja ambos binarios.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/build-sidecar.sh && file src-tauri/binaries/asr-x86_64-apple-darwin | grep -q x86_64 && file src-tauri/binaries/asr-aarch64-apple-darwin | grep -q arm64</automated>
  </verify>
  <done>
    build-sidecar.sh genera ambos triples; `file` confirma asr-x86_64-apple-darwin es Mach-O x86_64
    y asr-aarch64-apple-darwin es arm64. El script termina con exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: release.yml en macos-15 + steps de build del sidecar por triple</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    .github/workflows/release.yml (archivo a modificar — líneas clave en current_state arriba).
    RESEARCH.md sección "Pasos exactos a añadir en CI" (Cambios 1-4) y "Orden en el workflow" (líneas 120-171).
  </read_first>
  <action>
    Editar .github/workflows/release.yml:

    1) Cambiar `runs-on: macos-14` → `runs-on: macos-15` (línea 12). Actualizar el comentario adyacente
       que menciona "macos-14 = Apple Silicon (M1)" para que diga macos-15 (Xcode 26.x con SDK macOS 26,
       requerido por el Package.swift `.macOS("26.0")`).

    2) (Belt-and-suspenders, por si el default del runner cambia) Añadir un step de selección de Xcode
       DESPUÉS de checkout y ANTES de los builds:
         - name: Select Xcode 26.x
           run: sudo xcode-select -s /Applications/Xcode_26.3.app/Contents/Developer

    3) Añadir DOS steps de build del sidecar consecutivos, ANTES del step "Build arm64 app bundle"
       (línea 41). Ambos triples deben existir antes del primer tauri build:
         - name: Build ASR sidecar (arm64)
           run: |
             cd src-tauri/sidecar-asr
             swift build -c release
             mkdir -p ../binaries
             cp .build/arm64-apple-macosx/release/asr ../binaries/asr-aarch64-apple-darwin
             chmod +x ../binaries/asr-aarch64-apple-darwin
         - name: Build ASR sidecar (x86_64 cross-compiled)
           run: |
             cd src-tauri/sidecar-asr
             swift build -c release --triple x86_64-apple-macosx11.0
             cp .build/x86_64-apple-macosx/release/asr ../binaries/asr-x86_64-apple-darwin
             chmod +x ../binaries/asr-x86_64-apple-darwin

    NO reordenar los steps de firma/DMG existentes. NO tocar el step `rustup target add x86_64-apple-darwin`
    (línea 28-29) ni los TAURI_SIGNING_PRIVATE_KEY env (se mantienen vacíos — la firma real es Phase 3).
    Mantener YAML indentation consistente (2 espacios bajo steps).
  </action>
  <acceptance_criteria>
    - `grep -q "runs-on: macos-15" .github/workflows/release.yml` && `! grep -q "runs-on: macos-14" .github/workflows/release.yml`
    - `grep -c "swift build -c release" .github/workflows/release.yml` devuelve 2 (arm64 + x86_64)
    - `grep -q "x86_64-apple-macosx11.0" .github/workflows/release.yml`
    - `grep -q "asr-aarch64-apple-darwin" .github/workflows/release.yml` && `grep -q "asr-x86_64-apple-darwin" .github/workflows/release.yml`
    - El step "Build ASR sidecar (x86_64 cross-compiled)" aparece ANTES de "Build x86_64 app bundle"
      en el archivo (verificable: la línea del primero es menor que la del segundo).
    - El YAML parsea sin error: `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/release.yml'))"` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))" && grep -q "runs-on: macos-15" .github/workflows/release.yml && [ "$(grep -c 'swift build -c release' .github/workflows/release.yml)" = "2" ]</automated>
  </verify>
  <done>
    release.yml corre en macos-15, selecciona Xcode 26.3, y compila el sidecar para ambos triples
    antes de los respectivos tauri build. El YAML es válido.
  </done>
</task>

<task type="auto">
  <name>Task 3: confirmar tauri.conf.json (externalBin + targets) y verificación final</name>
  <files>src-tauri/tauri.conf.json</files>
  <read_first>
    src-tauri/tauri.conf.json (líneas 49-65 en current_state). RESEARCH.md "Verificación de COMPAT-01" (líneas 186-193).
  </read_first>
  <action>
    Revisar src-tauri/tauri.conf.json. La configuración actual YA es correcta para la estrategia de
    dos DMGs per-triple: `"externalBin": ["binaries/asr"]` (Tauri añade el sufijo de triple automáticamente)
    y `"targets": ["dmg", "app"]`. NO cambiar `externalBin` ni `minimumSystemVersion` (11.0 es correcto:
    la app Tauri corre en macOS 11+; solo el sidecar Apple es macOS 26, gateado en runtime).

    Acción concreta: confirmar (sin modificar salvo que falte) que `externalBin` contiene exactamente
    `"binaries/asr"` y que NO hay un sufijo de triple hardcodeado. Si todo está correcto, este task es
    una confirmación + la verificación end-to-end del plan (no requiere edición de config).

    Verificación final del plan (no requiere hardware Intel): correr build-sidecar.sh y confirmar que
    ambos binarios existen con el arch correcto. Confirmar que el bundle Tauri arm64 nativo aún resuelve
    el sidecar — `cargo build` desde src-tauri compila sin romper (no hace bundle, solo valida que el
    crate Rust sigue compilando tras los cambios de Plan 01, que son solo scripts/yaml/config).
  </action>
  <acceptance_criteria>
    - `grep -q '"binaries/asr"' src-tauri/tauri.conf.json` (externalBin sin sufijo de triple)
    - `! grep -q "asr-x86_64\|asr-aarch64" src-tauri/tauri.conf.json` (NO hay triple hardcodeado en config)
    - `ls src-tauri/binaries/asr-aarch64-apple-darwin src-tauri/binaries/asr-x86_64-apple-darwin` lista ambos
    - `file src-tauri/binaries/asr-x86_64-apple-darwin` contiene "x86_64"
    - `cd src-tauri && cargo build` exit 0 (el crate compila tras los cambios de scripts/yaml/config)
  </acceptance_criteria>
  <verify>
    <automated>ls src-tauri/binaries/asr-x86_64-apple-darwin src-tauri/binaries/asr-aarch64-apple-darwin && file src-tauri/binaries/asr-x86_64-apple-darwin | grep -q x86_64 && cd src-tauri && cargo build 2>&1 | tail -3</automated>
  </verify>
  <done>
    tauri.conf.json confirmado correcto (externalBin sin sufijo). Ambos binarios sidecar presentes con
    arch correcto. `cargo build` compila. COMPAT-01 verificado hasta donde permite el hardware (sin boot Intel).
  </done>
</task>

</tasks>

<verification>
- `bash scripts/build-sidecar.sh` produce ambos triples sin error.
- `file src-tauri/binaries/asr-x86_64-apple-darwin` reporta x86_64; el aarch64 reporta arm64.
- release.yml: macos-15, dos `swift build`, YAML válido, sidecar antes de cada tauri build.
- `cd src-tauri && cargo build` compila.
</verification>

<success_criteria>
COMPAT-01 cubierto: el sidecar x86_64 se cross-compila y el archivo se produce; build-sidecar.sh
y release.yml generan ambos triples; CI usa macos-15. El DMG Intel completo se valida en CI (no localmente).
</success_criteria>

<output>
Tras completar, crear `.planning/phases/02-compatibilidad-honesta-intel-apple-silicon/02-01-SUMMARY.md`
</output>
