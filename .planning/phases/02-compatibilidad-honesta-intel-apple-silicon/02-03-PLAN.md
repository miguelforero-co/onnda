---
phase: 02-compatibilidad-honesta-intel-apple-silicon
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/lib/types.ts
  - src/lib/components/ModelCard.svelte
  - src/routes/widget/+page.svelte
autonomous: false
requirements: [COMPAT-02, COMPAT-05]
must_haves:
  truths:
    - "La tarjeta del motor Apple aparece deshabilitada con un mensaje en español (tooltip/subtítulo) cuando disabled_reason está presente"
    - "Una tarjeta con disabled_reason no es clickeable ni seleccionable"
    - "El widget NO muestra el flash de forma-notch en el primer frame en pantallas sin notch"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "ModelInfo TypeScript con disabled_reason"
      contains: "disabled_reason"
    - path: "src/lib/components/ModelCard.svelte"
      provides: "Render del estado deshabilitado-con-explicación"
      contains: "disabled_reason"
    - path: "src/routes/widget/+page.svelte"
      provides: "hasNotch init en false (sin flash)"
      contains: "hasNotch = $state(false)"
  key_links:
    - from: "src/lib/components/ModelCard.svelte"
      to: "model.disabled_reason"
      via: "branch que muestra badge/subtítulo y bloquea la selección"
      pattern: "disabled_reason"
    - from: "src/routes/widget/+page.svelte"
      to: "evento screen-notch"
      via: "hasNotch arranca false y transiciona a true solo si llega el evento"
      pattern: "hasNotch = \\$state\\(false\\)"
---

<objective>
Dos cambios de frontend que cierran la honestidad de UI:
- COMPAT-02 (UI): la tarjeta del motor Apple, cuando el backend la marca con `disabled_reason`,
  se muestra deshabilitada con la explicación en español y no es seleccionable (en vez de aparecer
  "disponible" y fallar al usarla en Intel/macOS<26).
- COMPAT-05: el widget arranca con `hasNotch = false` para eliminar el flash de forma-notch en el
  primer frame en pantallas sin notch.

Purpose: completar la mitad de UI de COMPAT-02 (el backend ya emite disabled_reason en Plan 02) y
arreglar el artefacto visual del notch que se ve en MacBooks Intel y monitores externos.
Output: types.ts con el campo, ModelCard.svelte renderizando el estado deshabilitado, widget con
init correcto.

Depende de Plan 02: el campo `disabled_reason` debe existir en la serialización Rust de ModelInfo
antes de que el frontend lo consuma. (Wave 2.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-compatibilidad-honesta-intel-apple-silicon/02-RESEARCH.md
@.planning/phases/02-compatibilidad-honesta-intel-apple-silicon/02-CONTEXT.md
@.planning/phases/02-compatibilidad-honesta-intel-apple-silicon/02-02-PLAN.md

<interfaces>
<!-- Contratos exactos. Extraídos del código. NO explorar el codebase. -->

types.ts ModelInfo actual (types.ts:47-53) — añadir disabled_reason:
```typescript
export interface ModelInfo {
  id: string;
  name: string;
  size_mb: number;
  downloaded: boolean;
  coming_soon: boolean;
  // AÑADIR (Rust Option<String> serializa a string | null):
  disabled_reason: string | null;
}
```

ModelCard.svelte estado actual (claves):
- props (líneas 6-22): { model, selected, progress, error, comingSoon, onDownload, onSelect }
- línea 24: `const selectable = $derived(model.downloaded && !comingSoon);`
- línea 30-35: class:selected / class:coming-soon, role, tabindex, onclick, onkeydown gobernados por selectable/comingSoon
- línea 40: `{#if comingSoon}<span class="sub">Disponible en una próxima versión.</span>{/if}`
- línea 43-55: model-action — comingSoon → badge "Próximamente"; downloaded+selected → "Activo"; etc.
- CSS existente reutilizable: `.model-card.coming-soon { opacity:.55; cursor:default; }`,
  `.badge.soon`, `.model-info .sub`.

ModelCard se usa en DOS sitios (ambos pasan `model={m}`):
- src/lib/sections/Ajustes.svelte:206-214 (pasa comingSoon={m.coming_soon})
- src/routes/+page.svelte:231-236 (onboarding; NO pasa comingSoon)
NOTA: `disabled_reason` viaja DENTRO de `model` (model.disabled_reason), así que NO requiere un prop
nuevo ni cambios en los call-sites. Manejarlo internamente en ModelCard leyendo model.disabled_reason.

widget/+page.svelte estado actual:
- línea 14: `let hasNotch = $state(true);`  ← cambiar a false
- línea 327 (onMount): `await listen<boolean>("screen-notch", ...)` actualiza hasNotch
- línea 354: el div usa `class:no-notch={!hasNotch}` (o similar) — hasNotch solo se usa en :14, :327, :354
- La transición CSS de píldora→notch ya existe (clip-path animado). Con init false, en pantallas con
  notch real la primera transición es píldora→notch (aceptable, springy); en pantallas sin notch se
  queda en false (sin flash). Ver RESEARCH.md COMPAT-05.
</interfaces>

<facts>
- Rust `Option<String>` serializa con serde a `string | null` en JSON (None → null).
- El estado "deshabilitado por hardware" (disabled_reason presente) debe comportarse como coming_soon:
  no clickeable, muted, pero mostrando el mensaje real en vez de "Próximamente".
- `disabled_reason` es para modelos que EXISTEN pero no en este hardware; `coming_soon` para modelos
  que aún no existen (Parakeet). Son estados distintos — no fusionar la lógica, sí compartir el CSS muted.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (COMPAT-02 UI): types.ts + ModelCard renderiza disabled_reason</name>
  <files>src/lib/types.ts, src/lib/components/ModelCard.svelte</files>
  <read_first>
    src/lib/types.ts líneas 47-53 (ModelInfo). src/lib/components/ModelCard.svelte completo
    (props, selectable, branches de model-info y model-action, CSS .coming-soon/.badge.soon/.sub).
    Snippets en <interfaces> arriba. RESEARCH.md "Cómo surface a la UI" (líneas 310-331).
  </read_first>
  <action>
    1) types.ts (línea 52, dentro de interface ModelInfo): añadir
       `disabled_reason: string | null;`

    2) ModelCard.svelte:
       a) `selectable` (línea 24): incluir la nueva condición →
          `const selectable = $derived(model.downloaded && !comingSoon && !model.disabled_reason);`
       b) Añadir un derived para el estado deshabilitado-por-hardware:
          `const hardwareDisabled = $derived(!!model.disabled_reason);`
       c) En el div raíz: aplicar la clase muted también cuando hardwareDisabled →
          `class:coming-soon={comingSoon || hardwareDisabled}` (reusa el CSS .coming-soon ya muted).
          El role/tabindex/onclick/onkeydown ya dependen de `selectable`, que ahora es false si hay
          disabled_reason — no requiere cambios adicionales ahí.
       d) En model-info, tras el `{#if comingSoon}` subtítulo, añadir el subtítulo de hardware:
          `{#if hardwareDisabled}<span class="sub" title={model.disabled_reason}>{model.disabled_reason}</span>{/if}`
       e) En model-action, añadir una rama ANTES de la de comingSoon (o ajustar el if/else if) para que
          cuando hardwareDisabled muestre un badge "No disponible":
          `{#if hardwareDisabled}<span class="badge soon">No disponible</span>{:else if comingSoon}...`
          Reusar la clase `.badge.soon` existente.

    NO cambiar los call-sites (Ajustes.svelte, +page.svelte): disabled_reason viaja dentro de `model`.
    Mantener el comportamiento de coming_soon (Parakeet) intacto.
  </action>
  <acceptance_criteria>
    - `grep -q "disabled_reason: string | null" src/lib/types.ts`
    - `grep -q "model.disabled_reason" src/lib/components/ModelCard.svelte`
    - `grep -q "hardwareDisabled" src/lib/components/ModelCard.svelte`
    - `grep -q "No disponible" src/lib/components/ModelCard.svelte`
    - `selectable` incluye `!model.disabled_reason`: `grep -q "!model.disabled_reason" src/lib/components/ModelCard.svelte`
    - `npm run check` exit 0 (svelte-check + tsc sin errores)
  </acceptance_criteria>
  <verify>
    <automated>npm run check 2>&1 | tail -8</automated>
  </verify>
  <done>
    types.ts tiene disabled_reason; ModelCard muestra el mensaje en español como subtítulo + badge
    "No disponible", la tarjeta no es seleccionable cuando disabled_reason está presente, y coming_soon
    sigue funcionando. `npm run check` verde.
  </done>
</task>

<task type="auto">
  <name>Task 2 (COMPAT-05): hasNotch init false (sin flash de notch)</name>
  <files>src/routes/widget/+page.svelte</files>
  <read_first>
    src/routes/widget/+page.svelte líneas 12-17 (declaración de estado) y verificar que hasNotch solo
    se usa en :14 (init), :327 (listener screen-notch) y :354 (class binding). RESEARCH.md COMPAT-05
    "Fix mínimo" (líneas 477-494).
  </read_first>
  <action>
    Cambiar widget/+page.svelte línea 14:
      `let hasNotch = $state(true);`  →  `let hasNotch = $state(false);`

    Cambio de un valor. Efecto: el primer render usa la forma sin-notch (píldora); cuando llega el evento
    `screen-notch: true` (macOS con notch real) transiciona suavemente a la forma de notch; en pantallas
    sin notch se queda en false → sin flash. NO tocar el listener (línea ~327) ni el class binding
    (línea ~354) ni el CSS de transición. NO añadir cambios en Rust (lib.rs/shortcut.rs) — el init false
    es suficiente (ver Open Question 1 de RESEARCH, descartada).
  </action>
  <acceptance_criteria>
    - `grep -q "let hasNotch = \$state(false);" src/routes/widget/+page.svelte`
    - `! grep -q "let hasNotch = \$state(true);" src/routes/widget/+page.svelte`
    - `npm run check` exit 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'let hasNotch = $state(false);' src/routes/widget/+page.svelte && ! grep -q 'let hasNotch = $state(true);' src/routes/widget/+page.svelte && npm run check 2>&1 | tail -4</automated>
  </verify>
  <done>
    hasNotch arranca en false; el widget no muestra el flash de forma-notch en pantallas sin notch.
    `npm run check` verde.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: verificar UI del catálogo y ausencia de flash de notch</name>
  <action>Pausa para verificación humana — el ejecutor presenta lo construido y espera la señal de reanudación. No hay automatización adicional en este task.</action>
  <what-built>
    Frontend de Phase 2: la tarjeta del motor Apple se muestra deshabilitada con explicación en español
    cuando el hardware no la soporta (COMPAT-02 UI), y el widget arranca sin el flash de forma-notch (COMPAT-05).
  </what-built>
  <how-to-verify>
    1. Corre `npm run tauri dev` (NO release build).
    2. COMPAT-05 (notch): activa el dictado con Alt+Espacio en una pantalla SIN notch (monitor externo,
       o el built-in de un MacBook sin notch). Confirma que el widget aparece como píldora SIN un parpadeo
       inicial de forma de notch. (En esta Mac Apple Silicon con notch, la transición inicial píldora→notch
       es aceptable y se ve intencional.)
    3. COMPAT-02 (UI): abre Ajustes → Modelos. Como estamos en Apple Silicon + macOS 26 con el sidecar
       presente, el motor Apple debería aparecer NORMAL (seleccionable). Para verificar el estado
       deshabilitado sin un Intel, opcionalmente comenta temporalmente la condición del sidecar en
       get_models o usa un build x86_64 — o confirma visualmente que un modelo con disabled_reason (si lo
       fuerzas) muestra el subtítulo en español + badge "No disponible" + no es clickeable.
    4. Confirma que la tarjeta "Parakeet" Próximamente (coming_soon) sigue mostrándose igual que antes.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe lo que se ve mal (flash de notch, tarjeta mal renderizada).</resume-signal>
</task>

</tasks>

<verification>
- `npm run check` exit 0 tras ambos cambios.
- ModelCard renderiza disabled_reason como subtítulo + badge, no seleccionable.
- widget/+page.svelte hasNotch arranca en false.
- Checkpoint humano confirma sin-flash y render correcto.
</verification>

<success_criteria>
COMPAT-02 (UI) y COMPAT-05 cubiertos: catálogo honesto en la UI (motor Apple deshabilitado con
explicación donde no aplica) y widget sin flash de notch. `npm run check` verde + verificación humana.
</success_criteria>

<output>
Tras completar, crear `.planning/phases/02-compatibilidad-honesta-intel-apple-silicon/02-03-SUMMARY.md`
</output>
