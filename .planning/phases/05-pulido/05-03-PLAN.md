---
phase: 05-pulido
plan: 03
type: execute
wave: 2
depends_on: [05-02]
files_modified:
  - src-tauri/src/commands.rs
  - src-tauri/src/paste.rs
  - src-tauri/src/models.rs
  - src-tauri/src/recording.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - POLISH-02
  - POLISH-03
must_haves:
  truths:
    - "commands.rs queda partido en paste.rs (clipboard+CGEvent), models.rs (catálogo+descarga+resolución de path) y recording.rs (máquina de estados+streaming+tail)"
    - "La lógica de resolución de modelo (ggml-{model}.bin / fallback ggml-base.bin / exists) duplicada 4× se reemplaza por una función resolve_model_path() en models.rs, llamada desde los 4 sitios"
    - "resolve_model_path tiene tests de lógica pura (acompañan a la extracción, POLISH-02)"
    - "El refactor es behavior-preserving: cargo build verde, todos los tests pasan (conteo >= pre-refactor), npm run check 0 errores"
    - "invoke_handler en lib.rs sigue registrando los 20 comandos de commands.rs en su nueva ubicación"
  artifacts:
    - path: "src-tauri/src/paste.rs"
      provides: "paste_text + write_clipboard_utf8 + post_cmd_v"
      contains: "pub"
    - path: "src-tauri/src/models.rs"
      provides: "get_models, download_model, check_model_status, resolve_model_path, models_dir, ModelInfo, ModelStatus"
      contains: "resolve_model_path"
    - path: "src-tauri/src/recording.rs"
      provides: "start_recording_internal, stop_and_transcribe_internal, cancel_recording_internal, statics CAPTURE/COMMITTED_TEXT/etc"
      contains: "start_recording_internal"
  key_links:
    - from: "src-tauri/src/recording.rs (stop_and_transcribe_internal)"
      to: "models::resolve_model_path"
      via: "llamada a la función deduplicada"
      pattern: "resolve_model_path"
    - from: "src-tauri/src/lib.rs (invoke_handler)"
      to: "commands::* (wrappers #[tauri::command])"
      via: "tauri::generate_handler!"
      pattern: "commands::(get_models|download_model|start_recording|stop_and_transcribe)"
---

<objective>
Partir el god-file `commands.rs` (~1017 LOC) en módulos enfocados — `paste.rs`, `models.rs`, `recording.rs` — SIN cambiar comportamiento, y extraer la lógica de resolución de path de modelo (hoy duplicada 4×) en un único `resolve_model_path()` testeado.

Purpose: `commands.rs` mezcla 20 comandos + máquina de estados de grabación + paste + descarga + catálogo. Partirlo en módulos enfocados hace el código mantenible para un proyecto OSS (POLISH-03), y deduplicar `resolve_model_path` elimina la duplicación LOW del diagnóstico y crea un helper puro testeable (POLISH-02).
Output: 3 módulos nuevos + commands.rs adelgazado a wrappers `#[tauri::command]` + lib.rs actualizado. Build verde, ~49+ tests pasan, npm check 0/0.

CRÍTICO — REFACTOR MECÁNICO: NO cambiar lógica. Mover bloques y ajustar visibilidad/paths. El gate es behavior-preserving: build + todos los tests + npm check pasan idénticos. Los tests de Wave 1 (05-02) + los 44 previos blindan el movimiento — por eso este plan depende de 05-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-pulido/05-CONTEXT.md

# El god-file a partir (leer COMPLETO antes de mover nada)
@src-tauri/src/commands.rs

# Registro de módulos + invoke_handler (actualizar mod decls y confirmar comandos)
@src-tauri/src/lib.rs

# Patrón de #[cfg(test)] mod tests para los tests de resolve_model_path
@src-tauri/src/compat.rs

<interfaces>
Mapa de extracción (de commands.rs actual, 1017 LOC):

paste.rs ← mover:
- `fn paste_text(text: &str)` (línea ~945)
- `unsafe fn write_clipboard_utf8(text: &str)` (~959) [#[cfg(target_os="macos")]]
- `unsafe fn post_cmd_v()` (~974) [#[cfg(target_os="macos")]]
- `fn ax_is_trusted() -> bool` (~25) — usado por test_paste y check_accessibility_permission; ponerlo en paste.rs como `pub(crate)` y referenciarlo desde commands.rs, O dejarlo en commands.rs y exponerlo. Claude's Discretion; debe seguir compilando ambos call-sites.

models.rs ← mover:
- `fn models_dir<R>(app) -> Option<PathBuf>` (~746)
- `fn get_models<R>(app) -> Vec<ModelInfo>` (~751) [#[tauri::command]]
- `async fn download_model<R>(app, model_id) -> Result<(),String>` (~823) [#[tauri::command]]
- `fn check_model_status<R>(app) -> ModelStatus` (~923) [#[tauri::command]]
- structs `ModelInfo` (~1003) y `ModelStatus` (~913)
- NUEVO: `fn resolve_model_path(dir: &Path, model_name: &str) -> Option<PathBuf>` que encapsula:
    `primary = dir.join("ggml-{model_name}.bin"); fallback = dir.join("ggml-base.bin");
     if primary.exists() { Some(primary) } else if fallback.exists() { Some(fallback) } else { None }`

models.rs invoke_handler note: get_models/download_model/check_model_status son #[tauri::command]. Pueden:
  (a) moverse a models.rs y registrarse como `models::get_models` en lib.rs, O
  (b) quedar como wrapper delgado en commands.rs que llama a models::*.
Claude's Discretion — pero invoke_handler DEBE seguir registrando los 20 comandos sin omitir ninguno.

recording.rs ← mover (la máquina de estados + statics + streaming):
- statics: `IS_RECORDING`, `CAPTURE`, `COMMITTED_TEXT`, `COMMITTED_SAMPLES`, `STREAM_HANDLE` (~11-22)
- `fn start_recording_internal<R>(app) -> Result<(),String>` (~100, incluye el streaming loop spawned)
- `async fn stop_and_transcribe_internal<R>(app)` (~262)
- `async fn cancel_recording_internal<R>(app)` (~458)
- `fn is_recording() -> bool` (~517)
- `fn transcribe_file<R>(app, path)` (~556) [#[tauri::command]] — también usa resolve_model_path
- recording.rs usa: paste::paste_text, models::{models_dir, resolve_model_path}

commands.rs queda con:
- los 4 comandos de permisos (check_mic_permission, check_accessibility_permission, open_accessibility_settings, open_microphone_settings)
- get_settings, save_settings
- wrappers #[tauri::command]: start_recording, stop_and_transcribe, is_recording_cmd (llaman a recording::*)
- test_paste, get_build_hash, get_app_version
- hide_widget
- comandos de history (get_history, delete_history_entry, correct_history_entry, get_recording_audio)
- re-exports / `pub use` según haga falta para que lib.rs no cambie las rutas de los comandos (Claude's Discretion: mover el #[tauri::command] o re-exportar).

Los 4 sitios duplicados de resolución de path a reemplazar por resolve_model_path():
  1. start_recording_internal streaming loop (commands.rs ~161-170)
  2. stop_and_transcribe_internal (~341-357)
  3. transcribe_file (~601-617)
  4. la closure `model_exists`/exists usada en get_models y check_model_status (~937-939) — al menos check_model_status (~937) usa el mismo patrón primary/fallback y debe usar resolve_model_path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extraer paste.rs y models.rs (con resolve_model_path deduplicado + tests)</name>
  <read_first>
    - src-tauri/src/commands.rs COMPLETO (entender qué mueve cada bloque, líneas citadas en <interfaces>)
    - src-tauri/src/lib.rs (mod decls líneas 8-32, invoke_handler 58-85)
    - src-tauri/src/compat.rs (patrón #[cfg(test)] mod tests)
  </read_first>
  <files>src-tauri/src/paste.rs, src-tauri/src/models.rs, src-tauri/src/commands.rs, src-tauri/src/lib.rs</files>
  <action>
    1. Crear `src-tauri/src/paste.rs`: mover `paste_text`, `write_clipboard_utf8`, `post_cmd_v` (y `ax_is_trusted` si decides centralizarlo aquí). Marcar `paste_text` y los helpers que otros módulos necesiten como `pub(crate)`. Mantener los `#[cfg(target_os = "macos")]` idénticos.
    2. Crear `src-tauri/src/models.rs`: mover `models_dir`, `get_models`, `download_model`, `check_model_status`, structs `ModelInfo` y `ModelStatus`. Añadir `pub(crate) fn resolve_model_path(dir: &std::path::Path, model_name: &str) -> Option<std::path::PathBuf>` con la lógica primary/fallback/exists descrita en <interfaces>. Reemplazar el bloque primary/fallback dentro de `check_model_status` por una llamada a `resolve_model_path(&dir, &model_name).is_some()`. Marcar `models_dir` y `resolve_model_path` como `pub(crate)` (los usa recording.rs).
    3. Añadir a models.rs un `#[cfg(test)] mod tests` que testee `resolve_model_path` con tmpdir (usar `std::env::temp_dir()` o crear un dir temporal): (a) ni primary ni fallback existen → None; (b) solo fallback (ggml-base.bin) existe → Some(fallback); (c) primary (ggml-{model}.bin) existe → Some(primary) y tiene prioridad sobre fallback. Crear/borrar archivos vacíos para simular `exists()`. Imitar el patrón de compat.rs.
    4. Actualizar `lib.rs`: añadir `mod paste;` y `mod models;`. Si mueves los #[tauri::command] (get_models/download_model/check_model_status) a models.rs, cambiar sus entradas en invoke_handler a `models::get_models`, etc. Si los dejas como wrapper en commands.rs, no cambiar invoke_handler. Confirmar que los 20 comandos siguen registrados.
    5. En commands.rs: eliminar los bloques movidos; ajustar referencias (`crate::paste::paste_text`, `crate::models::*`). Mantener todo lo que no se movió.
    NO cambiar ninguna lógica. Solo mover y ajustar paths/visibilidad.
  </action>
  <verify>
    <automated>cd src-tauri && cargo build 2>&1 | grep -Eq "Finished|Compiling" && cargo test models 2>&1 | grep -Eq "test result: ok" && echo MODELS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src-tauri/src/paste.rs && test -f src-tauri/src/models.rs` → exit 0
    - `grep -q "resolve_model_path" src-tauri/src/models.rs` → exit 0 (función creada)
    - `grep -c "ggml-base.bin" src-tauri/src/commands.rs` → 0 (la lógica de fallback ya NO está duplicada en commands.rs; vive en resolve_model_path)
    - `grep -q "mod paste" src-tauri/src/lib.rs && grep -q "mod models" src-tauri/src/lib.rs` → exit 0
    - `grep -q "mod tests" src-tauri/src/models.rs` y `grep -c "#\[test\]" src-tauri/src/models.rs` ≥3 → tests de resolve_model_path presentes
    - `cd src-tauri && cargo build` → exit 0, "Finished"
    - `cd src-tauri && cargo test models` → `test result: ok`, ≥3 tests, 0 failed
  </acceptance_criteria>
  <done>paste.rs y models.rs existen; resolve_model_path centraliza la lógica primary/fallback y tiene ≥3 tests que pasan; check_model_status la usa; lib.rs declara los nuevos módulos; cargo build verde y commands.rs ya no contiene "ggml-base.bin".</done>
</task>

<task type="auto">
  <name>Task 2: Extraer recording.rs (statics + máquina de estados + streaming + tail) y adelgazar commands.rs</name>
  <read_first>
    - src-tauri/src/commands.rs (estado tras Task 1) — bloques de recording: statics ~11-22, start_recording_internal ~100, stop_and_transcribe_internal ~262, cancel_recording_internal ~458, is_recording ~517, transcribe_file ~556
    - src-tauri/src/models.rs (Task 1) — para usar models::{models_dir, resolve_model_path}
    - src-tauri/src/paste.rs (Task 1) — para usar paste::paste_text
    - src-tauri/src/lib.rs invoke_handler — confirmar registro de los 20 comandos
  </read_first>
  <files>src-tauri/src/recording.rs, src-tauri/src/commands.rs, src-tauri/src/lib.rs</files>
  <action>
    1. Crear `src-tauri/src/recording.rs`: mover los statics (`IS_RECORDING`, `CAPTURE`, `COMMITTED_TEXT`, `COMMITTED_SAMPLES`, `STREAM_HANDLE`), `start_recording_internal` (con su streaming loop spawned), `stop_and_transcribe_internal`, `cancel_recording_internal`, `is_recording`, y `transcribe_file`. Marcar como `pub(crate)` los que se usan fuera (start/stop/cancel internals, is_recording, transcribe_file).
    2. Dentro de recording.rs, reemplazar los 3 bloques restantes de resolución de path (en el streaming loop de start_recording_internal, en stop_and_transcribe_internal, y en transcribe_file) por llamadas a `crate::models::resolve_model_path(&dir, &model_name)`, preservando EXACTAMENTE los mensajes de error emitidos (`transcribe-error` / `file-transcribe-error`) y las ramas de Apple (is_apple → String::new()). El comportamiento observable no cambia.
    3. Reemplazar la llamada a `paste_text` por `crate::paste::paste_text`.
    4. En commands.rs: los wrappers #[tauri::command] `start_recording`, `stop_and_transcribe`, `is_recording_cmd` deben llamar a `recording::*`. `transcribe_file` (es #[tauri::command]): muévelo a recording.rs y regístralo como `recording::transcribe_file` en lib.rs, O déjalo como wrapper. Claude's Discretion.
    5. Actualizar lib.rs: `mod recording;`. Ajustar las entradas de invoke_handler que apunten a comandos movidos (ej. `recording::transcribe_file` si lo moviste). CONFIRMAR que los 20 comandos de commands originales + los 5 de data_mgmt/updater_check siguen TODOS registrados (25 entradas en generate_handler!).
    6. Confirmar que `cancel_recording_internal` sigue siendo llamado desde donde se invocaba (escape.rs / shortcut.rs) — ajustar la ruta a `crate::recording::cancel_recording_internal` si esos módulos lo importaban desde `crate::commands`.
    NO cambiar lógica. Refactor mecánico puro.
  </action>
  <verify>
    <automated>cd src-tauri && cargo build 2>&1 | grep -Eq "Finished" && cargo test 2>&1 | grep "test result: ok" | tail -1 && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src-tauri/src/recording.rs` → exit 0
    - `grep -q "start_recording_internal" src-tauri/src/recording.rs && grep -q "stop_and_transcribe_internal" src-tauri/src/recording.rs && grep -q "cancel_recording_internal" src-tauri/src/recording.rs` → exit 0
    - `grep -q "STREAM_HANDLE" src-tauri/src/recording.rs && grep -q "COMMITTED_TEXT" src-tauri/src/recording.rs` → statics movidos
    - `grep -c "ggml-base.bin" src-tauri/src/recording.rs` → 0 (recording.rs usa resolve_model_path, no duplica el fallback)
    - `grep -c "resolve_model_path" src-tauri/src/recording.rs` → ≥3 (los 3 sitios deduplicados: streaming loop, stop, transcribe_file)
    - `grep -q "mod recording" src-tauri/src/lib.rs` → exit 0
    - invoke_handler completo: `grep -c "::" <(sed -n '/generate_handler!/,/]);/p' src-tauri/src/lib.rs)` → 25 entradas (20 de commands/recording/models + reveal_data_dir, clear_history, clear_models, get_storage_usage, check_for_updates). Verificar que cada comando original sigue presente: `for c in get_settings save_settings start_recording stop_and_transcribe transcribe_file is_recording_cmd get_models download_model check_model_status check_mic_permission check_accessibility_permission open_accessibility_settings open_microphone_settings get_history delete_history_entry correct_history_entry get_recording_audio hide_widget test_paste get_build_hash get_app_version; do grep -q "$c" src-tauri/src/lib.rs || echo "MISSING $c"; done` → no imprime ningún MISSING
    - `cd src-tauri && cargo build` → exit 0, "Finished"
    - `cd src-tauri && cargo test 2>&1 | grep "test result"` → 0 failed en todas las suites; suma total de tests pasados ≥ el conteo previo a este plan (≥49, contando los nuevos de resolve_model_path)
    - `wc -l < src-tauri/src/commands.rs` → significativamente menor que 1017 (objetivo: <500; el grueso se movió a los 3 módulos)
  </acceptance_criteria>
  <done>recording.rs contiene la máquina de estados completa con sus statics, usa resolve_model_path (sin duplicar fallback); lib.rs declara mod recording y registra los 25 comandos sin omitir ninguno; cargo build verde; todos los tests pasan (≥49, 0 failed); commands.rs reducido a <500 LOC.</done>
</task>

<task type="auto">
  <name>Task 3: Verificación behavior-preserving end-to-end (build + tests + svelte-check)</name>
  <read_first>
    - src-tauri/src/lib.rs (invoke_handler final)
    - package.json (script check)
  </read_first>
  <files>src-tauri/src/commands.rs</files>
  <action>
    Verificación final de que el refactor preserva comportamiento, sin cambios de lógica:
    1. `cd src-tauri && cargo build` → debe terminar con "Finished", 0 errores.
    2. `cd src-tauri && cargo test` → todas las suites con `test result: ok`, 0 failed, conteo total ≥49.
    3. `npm run check` (svelte-check) → 0 errores, 0 warnings nuevos (el frontend no se tocó, debe seguir igual).
    4. Auditoría manual de invoke_handler: confirmar que cada uno de los 25 comandos está registrado y apunta a un símbolo que existe (cargo build ya lo garantiza, pero documentar en el SUMMARY la lista de comandos y su nueva ubicación de módulo).
    Si algo falla, corregir el path/visibilidad — NUNCA cambiar la lógica para que pase un test. Si un test que pasaba antes ahora falla, es señal de que el movimiento cambió comportamiento: revertir ese cambio puntual.
  </action>
  <verify>
    <automated>cd src-tauri && cargo build 2>&1 | grep -q "Finished" && cargo test 2>&1 | grep -q "test result: ok" && ! (cargo test 2>&1 | grep -qE "FAILED|test result: FAILED") && cd .. && npm run check 2>&1 | grep -Eiq "0 error|svelte-check found 0" && echo ALL_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `cd src-tauri && cargo build 2>&1 | grep -q "Finished"` → exit 0
    - `cd src-tauri && cargo test 2>&1 | grep -c "test result: ok"` → ≥1 y `cargo test 2>&1 | grep -c "FAILED"` → 0
    - Conteo total de tests pasados ≥49 (44 pre-fase + ≥5 vad + ≥3 resolve_model_path)
    - `npm run check 2>&1` → reporta 0 errores (cadena "0 error" o "found 0 errors and 0 warnings")
    - invoke_handler: el loop de comandos del Task 2 no imprime ningún "MISSING"
    - El frontend (`src/`) NO fue modificado: `git diff --name-only | grep -q "^src/" && echo CHANGED || echo UNCHANGED` → UNCHANGED (solo cambian src-tauri/src/*.rs)
  </acceptance_criteria>
  <done>cargo build verde, cargo test 0 fallos con ≥49 tests, npm run check 0 errores, los 25 comandos registrados verificados, y el frontend intacto — el refactor preserva el comportamiento por completo.</done>
</task>

</tasks>

<verification>
- `cd src-tauri && cargo build` termina con "Finished" (0 errores).
- `cd src-tauri && cargo test` → 0 failed, total ≥49 tests (44 previos + vad + resolve_model_path).
- `npm run check` → 0 errores.
- invoke_handler en lib.rs registra los 25 comandos; ninguno omitido tras el movimiento.
- `grep -c "ggml-base.bin"` en commands.rs y recording.rs combinados → la cadena solo aparece dentro de `resolve_model_path` en models.rs (una sola definición de fallback).
- commands.rs reducido de 1017 a <500 LOC.
</verification>

<success_criteria>
- POLISH-03 satisfecho: commands.rs partido en paste.rs / models.rs / recording.rs, behavior-preserving.
- POLISH-02 (parte refactor) satisfecho: resolve_model_path extraído y testeado (≥3 tests), eliminando la duplicación 4×.
- Build + tests + svelte-check pasan idénticos al estado pre-refactor.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pulido/05-03-SUMMARY.md` incluyendo la lista de los 25 comandos y el módulo donde quedó cada uno.
</output>
